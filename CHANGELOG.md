# 変更履歴

このファイルの書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に、
バージョン番号は [Semantic Versioning](https://semver.org/lang/ja/) に従います。

3.8.0 以前の変更は `git log` を参照してください。

## [5.0.0] - 2026-07-25

2026-07-24 にリリースされた **Claude Opus 5** に対応し、モデル選定の対象を
**Fable 5 / Opus 5 / Sonnet 5** に更新しました。Opus 5 は挙動が Opus 4.8 から大きく変わっている
ため（thinking の既定、検証指示の扱い、応答の長さ、委譲の積極性）、判断表とプロンプト最適化の
ロジックも合わせて変更しています。**選定対象モデルとガイドのファイル名が変わるため、メジャー
バージョンを上げました。**

### Changed

- **選定対象を Fable 5 / Opus 5 / Sonnet 5 に変更しました。** 「迷ったら Opus」の既定が公式に
  Opus 5 へ移ったため（Models overview / Choosing a model）、判断表の Opus 列をすべて Opus 5 に
  差し替えました。**effort は `high`（既定）から始め、demanding なコーディング・エージェント作業
  （複数ファイル機能・大きめのリファクタ・端から端までの実装・深い依存チェーンの追跡）で `xhigh` へ
  上げる**設計にしました（公式の推奨開始点。Opus 4.8 / 4.7 は「xhigh 開始」でモデルにより異なります）。
- **`04_opus48_prompting.md` を `04_opus5_prompting.md` にリネームし、全面改訂しました。**
  Opus 5 の破壊的変更（thinking が既定 on、`thinking:{"type":"disabled"}` と effort `xhigh`/`max`
  の併用は 400 エラー）と挙動差分を反映しています。
- **最適化プロンプトに「重複する自己検証の指示」を入れなくなりました（Opus 5）。** Opus 5 は
  指示なしで自己検証・自己修正するため、公式が削除を求める3つの型（①最終検証ステップの追加
  ②検証用サブエージェントの起動 ③「double-check せよ」「答える前に再検証せよ」）は過剰検証を
  招きます。テンプレートから `Verification` 節を外し、品質の担保は完了定義（受入条件・通すべき
  テスト）の明示に一本化しました。**完了定義の明示と「推測に頼らずコードから確認せよ」型の
  根拠接地の指示は削除対象ではありません**（別工程の検証を足すものではないため）。
  **Fable 5 は従来どおり**、長時間実行では検証方法・間隔・別コンテキストの検証サブエージェントを
  明示します（公式ガイダンスがモデルで異なります）。
- **skill の実行契約を「ガイドの構成に従う」に変更しました。** 従来は全モデルで4要素
  （Fixed instruction / Variables / Output format / Verification）を必ず生成していましたが、
  テンプレートの構成をモデルガイド側に委ね、ガイドにない節を足さない方式にしました。
- **判断表に Opus 5 の低 effort 活用（分岐D）を追加しました。** Opus 5 の `low`/`medium` は旧
  Opus より強く、公式も「evals で品質が保てる範囲で積極的に使え」と述べています。ただし自前の
  評価で確認していない作業に常用しないよう `[Heuristic]` として条件を明示しています。
- **max effort のゲートをモデル別に書き分けました。** Opus 4.7 / 4.8 の「evals が `xhigh` で
  measurable headroom を示すときのみ」という明示ゲートは Opus 5 のセクションには存在せず、
  Opus 5 は「タスクが無制限のトークン支出に見合うとき」と表現されています。混同しないよう
  分離記載し、判断表で max を通常行に置かない方針は `[Heuristic]` として維持しました。

### Added

- **Opus 5 の運用注意を判断表に追加しました。** 応答・成果物が長い／スコープを広げがち／進捗
  ナレーションとサブエージェント委譲が多い、という癖への対処を「Opus 5 のプロンプト上の癖」節に
  まとめました。effort を下げても表示される応答は短くならない（長さはプロンプトで指定する）点も
  明記しています。
- **Claude Code の運用差分を追加しました。** Opus 5 は **v2.1.219 以降が必要**、`opus` エイリアス
  はプロバイダごとに解決先が異なる（Microsoft Foundry のみ Opus 4.6）、**Opus 5 には effort の
  default hold がなく前回値を持ち越す**（Fable 5 / Opus 4.8 / 4.7 は hold あり）ことを記載しました。
- **拒否時のフォールバック経路をサーフェス別・カテゴリ別に整理しました。** Claude Code では
  Fable 5 の biology → Opus 5、Fable 5 の cybersecurity → Opus 4.8、Opus 5 の cybersecurity →
  Opus 4.8、Opus 5 の biology はフォールバックなしで拒否。API 側はオプトイン（`fallbacks`）で、
  Fable 5 の推奨フォールバック先は現在も Opus 4.8 です。
- **プラン差の注記を追加しました。** Fable 5 は全有料プランで選択できますが課金形態が異なります
  （Max / Team Premium / legacy seat-based Enterprise Premium 席は**プラン内包・週間使用量上限の
  最大50%まで**、Pro / Team Standard 席は **usage credits** による従量課金）。「Pro では使えない」
  ではなく「usage credits が必要」と表現します。1M コンテキストは Sonnet 5 がネイティブ、
  Opus の 1M は Pro のみ usage credits が必要です。

### Documentation

- **根拠台帳に P16〜P21（What's new in Opus 5 / Prompting Claude Opus 5 / Model deprecations /
  Fast mode / API and data retention / Claude Fable 5 on your plan）と S81〜S106 を追加しました。**
  既存ページのうち **P1 / P2 / P5 / P7 / P9 / P11 / P12 / P13 / P14 / P15 を 2026-07-25 に直接取得で
  再検証**しました（P3 / P6 / P8 は今回未再取得で従来の検証日を維持、P4 は現在 P16 へリダイレクトされ
  直接取得不能、P10 は欠番）。
- **Opus 4.8 は「選定対象外・参照用」として台帳に残しました。** Opus 4.8 は公式に **Active**
  （暫定退役日は早くても 2027-05-28）で、フォールバック先・移行元・互換性の説明で参照するため、
  事実として正しい主張は削除していません。台帳の `Retired` は「この台帳の主張を置き換えた」と
  いう意味であり、モデルの廃止を意味しないことを明記しました。
- **ZDR の記述をモデル別・サーフェス別に精密化しました。** Covered Model（30日保持必須・ZDR 不可）
  は **Fable 5 と Mythos 5 のみ**です。Opus 4.8 は公式に "remains available under ZDR"、Sonnet 5 も
  ZDR 可。Opus 5 は Covered Model の指定がありませんが、ZDR 適格性は機能・サーフェス・組織契約に
  依存するため「全サーフェスで ZDR 可」とは書いていません。
- **公式ドキュメントの記述が同日内に変わった件を台帳に記録しました。** Opus 5 の effort 推奨は
  2026-07-25 の 06:54 時点の取得では「xhigh 開始」でしたが、同日 09:35 以降の再取得では
  「`high`（既定）開始・demanding な作業で `xhigh` へ昇格」になっていました（P2 / P13 / P16 / P17 の
  4ページ）。前後の逐語と取得時刻を「公式ページの記述が変わった記録」節に残しています。
  なお、この間に編集が行われたのか、先の取得が古い配信キャッシュだったのかは区別できないため、
  「公式が改訂した」とは断定していません。
- **`tools/CODEX_VERIFICATION_PROMPT.md` を全面更新しました。** 監査対象の P/S-id 範囲を
  P1〜P21 / S1〜S106 に更新し、既知の落とし穴に「Opus 4.8 と Opus 5 で方向が逆転している挙動」
  「thinking 既定の三者差」「Covered Model の "Both models" の読み違い」を追加しました。
- README のデモ用スクリーンショット 3 枚を 5.0.0 の画面で撮り直しました（代替候補が Opus 5 /
  effort=xhigh、`Last verified` が 2026-07-25 になっています）。代替テキストも実際の表示内容に
  合わせて書き直しました。

### Known limitations

- **貼り付けログによる汚染耐性は 4.1.0 から変わっていません。** 会話ログを丸ごと貼り付けて
  呼び出すと、ログ内の依頼や `y` に引きずられ、フェーズ1の提示を出さずに応答してしまう場合が
  あります。5.0.0 では `skills/pick/SKILL.md` の手順7b・7c を変更しましたが、**この観点の
  再測定は行っていません**（リリース前の確認は `tests/regression/pasted-transcript-input.txt` を
  1 回実行して不変条件を満たしたことの確認までで、成功率の測定ではありません）。
  4.1.0 候補での測定は 10 回中 8 回でフェーズ1を提示（2 回再発）。測定条件と履歴は
  `tests/regression/README.md` を参照してください。
- **対話型の Claude Code を対象としています。** headless（`claude -p`）での複数ターン利用は
  保証しません（`--session-id` / `--resume` を使えば動作しますが、サポート対象外です）。

## [4.1.0] - 2026-07-20

Anthropic 公式一次情報による effort ガイダンスの再監査を反映し、判断表を「第一・第二候補ごとの
effort」へ再構成しました。skill の実行契約も、モデルと effort の両方を扱う形に更新しています。

### Changed

- **判断表を第一候補・第二候補それぞれの effort に分離しました。** 従来の単一 effort 列を第一
  effort / 第二 effort の2列に分け、`medium〜high` のような範囲表記を明示的な分岐条件（分岐
  A/B/C）へ置き換えました。同名 effort はモデル間で同じ強度を意味しないため、モデルを替えるとき
  は effort もその行の値へ引き直します。
- **`y <model>` で確定 effort が連動します。** 第一/第二候補にそのモデルがあればその位置の
  effort を使い、行の候補に無いモデルなら既定の high にフォールバックしてその旨を表示します。
- **実行前の案内が `/model` と `/effort` の両方になりました。** effort は Claude Code でセッション
  をまたいで残るため、モデルと effort の両方を設定する運用に統一しました。

### Documentation

- 根拠台帳の複合 source_id `S7` を原子命題 `S71`〜`S76` に分割し、Claude Code の effort 運用
  （永続性・モデル間の非等価・ultracode / ultrathink の区別）と default-first 原則を `S77`〜`S80`
  として追加しました。max effort の公式な位置づけ（能力要件ベースの用途・Opus の eval ゲート・
  セッション限定）を明記し、判断表では max を通常の第一推奨に置かない方針を `[Heuristic]` として
  分離記載しています。
- クイックスタートで、ターミナル（PowerShell / コマンドプロンプト）と Claude Code の入力欄を明確に
  区別しました（スラッシュコマンドのコードブロックを `sh` から `text` へ変更）。あわせて、Claude Code の
  Native Install では Node.js が不要であること（npm 経由のときだけ Node.js 22+ が必要）と、未導入者向けの
  公式インストール導線、インストール後に `claude` で起動する手順、PowerShell で slash コマンドを打って
  しまったときのトラブルシューティングを追記しました。Claude Desktop の Code タブでもプラグインを導入
  できる点も明記しています。

### Known limitations

- **貼り付けログによる汚染耐性は完全ではありません。** 会話ログを丸ごと貼り付けて呼び出すと、
  ログ内の依頼や `y` に引きずられ、フェーズ1の提示を出さずに応答してしまう場合があります。
  4.1.0 候補での測定は 10 回中 8 回でフェーズ1を提示（2 回再発）。少数試行のため成功率や版間差
  としては解釈しません。測定条件と履歴は `tests/regression/README.md` を参照してください。

## [4.0.0] - 2026-07-17

**配布方法が変わります。** コピー方式（`install.sh` で `~/.claude/skills/` へ配置）をやめ、
Claude Code のプラグインとして配布します。これにより、更新の受け取りとアンインストールが
Claude Code の仕組みに乗ります。

### ⚠️ 破壊的変更

- **standalone のコピー方式から、Claude Code プラグイン配布へ移行しました。**
  導入は `/plugin marketplace add kazuyakurashima/which-model` →
  `/plugin install which-model@kazuyakurashima` になります。
- **正式な呼び出し名が `/which-model` から `/which-model:pick` に変わりました。**
  プラグインのスキルは名前空間つきで登録されるためです（`/pick` という短縮形も動きますが、
  公式に保証された呼び出し方ではないため、確実なのは `/which-model:pick` です）。
- **プロジェクト側の `docs/ai-model-guides/` は、実行時に読まれなくなりました。**
  判断材料はプラグインに同梱されたものだけを使います。プロジェクトごとの調整は 4.0.0 では
  できません（要望が確認できれば、明示的なオプトインとして再導入を検討します）。
- **`install.sh` は何もインストールしません。** 移行案内を表示して終了するだけのスクリプトに
  なりました。standalone 版を使い続ける場合は `git checkout v3.9.0` を使ってください。
- **旧 standalone 版とは共存できますが、別のコマンドとして読み込まれます**（`/which-model` と
  `/which-model:pick` が並びます）。standalone を外すと `/which-model` は使えなくなります。

### Added

- **判断材料（レシピ本）をプラグインに同梱しました。** インストール直後から、プロジェクトへの
  コピーなしで動きます。
- **フェーズ1の提示に、判断表の `Last verified` 日付を1行表示するようにしました。** モデルの
  仕様・価格は陳腐化します。更新が止まった版を使い続けても、古いことが分かるようにするためです。
- `tools/sync-bundled-guides.sh` を追加しました（`docs/ai-model-guides/` が正本。編集したら
  同梱コピーへ同期します。`--check` でズレを検出できます）。

### Changed

- **明示的に呼び出したときだけ起動するようになりました**（`disable-model-invocation`）。
  Claude が自動で判定を挟むことはありません。「重要なときだけ呼ぶ」という設計と実装を一致させました。

### Removed

- ルートの `SKILL.md` を `skills/pick/SKILL.md` へ移設しました。
- 旧 standalone 版の同期 hook（`.claude/settings.json` の PostToolUse）を削除しました。

### 既知の制限

- **長い指示に会話ログを貼り付けると、推奨モデルの提示を飛ばすことがあります。** 3.9.0 で対処
  しましたが**完全な解消ではなく**、確率的に再発します。4.0.0 の同梱一本化による明らかな劣化は、
  今回の3試行では観測されていません（`tests/regression/` に測定条件と記録があります）。
- **対話型の Claude Code を対象としています。** headless（`claude -p`）での複数ターン利用は
  保証しません（フェーズ2のガイド読み取りが権限で止まることを実測しています）。

## [3.9.0] - 2026-07-15

長い指示を渡したときに、推奨モデルが提示されないまま止まって見える問題への対応。
調査の結果、症状の異なる2つの問題が同じ「止まった」に見えていたことが分かりました。

### Changed

- **確定合図の受理条件を厳格化しました。** `y` / `y opus` / `y fable` / `y sonnet` は、
  メッセージ**全体**がそれだけで構成されているときにのみ確定合図として扱います。
  これまでは長文の一部に含まれる `y` も合図として解釈される余地がありました。
  `y お願いします` のように `y` で始まるが完全一致しない場合は、合図として受理せず、
  受理できる合図を再提示して停止します（直前の提示は有効なままなので、`y` を送り直せます）。
- **フェーズ1の判定を即断で行うよう指示を追加しました。** 判断表の該当行を引くだけの作業なので、
  指示が長くても本文の精査はしません。提示までの待ち時間を短くするためです。
- **重複した停止指示を削減しました。** 「やらないこと」節が絶対規則をほぼ再掲していたため、
  規則に含まれない補足のみを残しました。過剰指示は指示追従をかえって弱めます。

### Fixed

- **長文中に会話ログを貼り付けるとフェーズ1が飛ばされる問題に対処しました**
  （**完全な解消ではありません**）。貼り付けた過去の会話・モデル提案・`y`・ツール結果を、
  引用ではなく現在の会話状態として解釈してしまい、推奨モデルを提示せずに埋め込まれた指示へ
  回答することがありました。入力を分類対象のデータとして扱う規則0を追加しています。
  今回の再現試験では 3.8.0 の 0/3 提示から 3.9.0 の 9/10 提示へ改善しましたが、
  **再発を確認しています**（試行回数が少なく、成功率としては読めません。`tests/regression/`）。
- **フェーズ1の出力契約を明示しました。** 提示の4〜5行を本文として出力してから応答を終える
  ことを必須とし、本文なしで応答を終えることを禁じました。
- **README の「稼働中は読み取り専用なので、そもそも実行できません」という記述は誤りでした。**
  `allowed-tools: Read` は Read を事前許可する設定であり、他のツールを禁止するものではありません。
  提示だけで止まるのは SKILL.md の絶対規則によるもので、技術的な制約ではありません。

### Added

- README の「設計上の割り切り」に、提示までの待ち時間の実測範囲を追記しました。

### 補足：長さの上限が原因ではありません

900文字以上の実履歴18件のうち17件は、最終的に提案を返していました（8,357文字の成功例あり）。
提示までの待ち時間は数十秒〜2分超とばらつき、指示の文字数との単純な比例は見られませんでした。
セッションのコンテキスト量や effort も影響している可能性がありますが、確かめていません。
実際に提案が出なかったのは、会話ログを貼り付けた1件だけでした。
