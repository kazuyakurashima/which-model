# 変更履歴

このファイルの書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に、
バージョン番号は [Semantic Versioning](https://semver.org/lang/ja/) に従います。

3.8.0 以前の変更は `git log` を参照してください。

## [Unreleased]

選定対象モデルを **Fable 5 → Fable 5.1** へ置き換えました（2026-09-02 の Fable 5.1 リリースを
受けた対応）。5.0.0（Opus 4.8 → Opus 5）と同じく、選定対象モデルとガイドのファイル名が変わるため
メジャーバージョンです。判断表の構造（16行×3プロファイル・分岐・条件）・選択UI・合図の文法は
変更していません。

Opus 5 対応のときと違い、公式は「既存の Fable 5 プロンプトは変更なしで 5.1 でも良好に動く」
（P23）と明言しており、**検証指示のような方向逆転（Opus 5 の S91 型）は確認されていません**。
そのためこの版の中心は「推奨の書き換え」ではなく「**根拠の付け替え**」です：effort 開始点は
high のまま（S128）、**16行×3プロファイルでモデル×effort が変わったセルはゼロ**で、変わったのは
「Fable」が指す先（5 → 5.1）と、各セルの根拠 S-id の紐づけです。

### Breaking

- 判断表と skill の選定対象が Fable 5 から **Fable 5.1** に変わった。Fable 5 を使い続ける場合は
  `/model claude-fable-5` の明示指定が必要（Claude Code v2.1.255 以降は保存済みの
  `claude-fable-5` 設定も `fable` エイリアスへ自動書き換えされ、以後 5.1 に解決する — S138）
- `03_fable5_prompting.md` を **`03_fable51_prompting.md`** にリネームし全面改訂（8節構成・
  テンプレートの骨格は維持。Fable 5.1 は Claude Code v2.1.255 以降が必要）

### Added

- 根拠台帳に **P22〜P25 の4ページ**（What's new in Claude Fable 5.1 / Prompting Claude Fable 5.1 /
  5.1 移行ガイド / 公式発表）と **S122〜S139 の18件**を新設。すべて 2026-09-02 の WebFetch
  直接取得の逐語を根拠にした。主な新規事実：
  - ポジショニング：5.1 は「Anthropic の最も高能力な widely released モデル」で Fable 5 の後継。
    Fable 5 は P1 の "Legacy models (still available)" へ（S122・S123）
  - 「迷ったら Opus 5」は不変。ただし P1 の逐語が "for most workloads" に変わり、5.1 への
    昇格条件（demanding reasoning / long-horizon agentic work / Opus 5 の高 effort でも evals が
    届かない）が追記された（S81 修正・S123）
  - effort：5.1 も high 開始で不変。昇格先に **max が明示**された（"xhigh **or max** for the most
    capability-sensitive" — S128）。effort sweep は 5 から引き継がずやり直す（S129）
  - 価格：入出力 $10/$50 は同額、**キャッシュ読みが $0.25/MTok（base の 0.025 倍。従来の 1/4）**
    （S126）
  - **橋の主張**：「既存の Fable 5 プロンプトは変更なしで 5.1 でも良好に動く」（S133）と、
    橋の例外＝**挙動差7点**（並列ツール呼び出しの不安定化・進捗テキストの減少・low での検索
    省略・散文の濃さ・書式の減少・引用符なしの再現・全ファイル書き直し — S134）
  - 5.1 固有：途中停止対策の公式2ブロック（S135）／xhigh・max での長い成果物の注意（S136）／
    lead agent を待たせないサブエージェント設計（S137）／Claude Code の最小バージョン
    v2.1.255・`fable` エイリアス解決・自動書き換え（S138）／API 変更（forced tool use 廃止・
    thinking ブロックの一方向性と append-only 要件・turn-scoped system messages 等 — S139）
  - 誤検知は 5 のローンチ時より減少し「ソースコードの脆弱性発見は許可」が明文化。Claude Code の
    cyber 介入は約60%減（公式発表）。許可フォールバック先は Opus 4.8 と Opus 5（S131・S132）
  - Covered Models は **5.1 / Mythos 5.1 / 5 / Mythos 5 の4モデル**に拡大（30日保持・ZDR 不可 —
    S127）。プラン記事は「Claude Fable models on your plan」に改題され両モデル同一扱い
    （週次上限 50% は Fable 系合算 — S105 拡張）

### Changed

- 台帳の既存 Fable 5 主張の処置（Retired の規約 01:18-21 に従う）：
  - **Retired 2件**：最高能力ポジショニングの2件（後継 S122/S123）。台帳の Retired は計5件に
  - **「選定対象外・参照用」化 15件**：値・経路・帰属が 5.1 で変わった主張（スペック・レイテンシ・
    ZDR・フォールバック・effort 開始点・対 4.8 比較群）
  - **橋適用 11件**：プロンプティング原則の対象欄を「Fable 5, Fable 5.1（S133 の橋で適用）」に
  - **直接拡張 5件**：公式が 5.1 への適用を明示するもの（adaptive 常時 on・display 仕様・
    分類器の3モデル列挙・プラン記事・トークナイザ）
  - **現行化修正**：effort レベル対応表（5.1/Mythos 5.1/Mythos Preview 追加）、fast mode
    （5.1 も非対応 — P19 で直接確認）、Covered Models の4モデル化、知識カットオフ最新（5.1 の
    2026-06）、per-message effort の例外、Claude Code のカテゴリ別フォールバック（5.1 追加）ほか
- 判断表（02）：Fable 系セルの根拠を 5.1 版 S-id へ引き直し。**#5（大規模リファクタ・移行）と
  #7（大規模レビュー）は S124 ①が行名をほぼ逐語で挙げるため根拠が強くなった**。#4/#8/#11 の
  成果優先は Fable 5 期の橋（S111/S114/S113）を S122/S124 で 5.1 に接続（#11 の Confidence は
  「対 Opus 5 の直接比較が無い」という決定的不確実性が残るため Low を維持）
- `tools/check-ledger-consistency.py`：検査4のモデル名照合を境界付き正規表現にした
  （"Fable 5" が "Fable 5.1" に部分一致して検査が無音で盲目化する罠の恒久対策）
- `tools/CODEX_VERIFICATION_PROMPT.md`：S/P 範囲を S1〜S139 / P1〜P25 に更新し、落とし穴に
  「Fable 5 と 5.1 の混同」（橋を誤りと判定しない・5.1 固有事実の一覧）を追加

### Fixed

- `tools/CODEX_VERIFICATION_PROMPT.md` の S-id 範囲が「S1〜S106」のまま S107〜S121 を
  含んでいなかった直し漏れ（2026-08-11 監査時の追随漏れ）を今回まとめて解消

### 根拠台帳（01_sources_evidence.md）

7.0.0 と同じく台帳を先に固定した（`commit 55cfd25`）。台帳は **139 番まで採番・135 件定義
（Active 130 / Retired 5）**。リリース当日の観測のため、5.0.0 の教訓（公式ページは数時間単位で
変わりうる）に従い、重要な推奨（effort 開始点・選定ガイダンス・フォールバック先）は逐語を
台帳に残し、タグ打ち前に再確認する。

### 変更していないもの

- 判断表の構造（16行×3プロファイル・分岐 A〜C・条件 1〜4・表示文の設計）と、Fable 系以外の
  セル（Opus 5 / Sonnet 5 の候補・effort・根拠）
- 選択UI・合図の文法（`p` / `g`、headless の `p quality` 等）。headless の受理トークン `fable` も
  据え置き（Claude Code 側の解決先が 5.1 に変わるだけ）
- 32セルの全再監査は行っていない（7.0.0 で実施済み。今回は Fable 系のセル・分岐・条件だけを
  再裁定した）
- 7.0.1 積み残し4件（縮退経路の合図揺れ等 — `tests/regression/README.md` の未決記録）は
  今回のスコープに含めていない

## [7.0.0] - 2026-08-13

**判断表の「第二候補」列を廃止し、候補を目的別の3プロファイル（通常推奨・成果優先・効率優先）へ
再構成しました**（**破壊的変更**）。あわせてタスク行を 13 → 16 に分割し、headless の確定合図を
変更しています。**裏付けの無い組み合わせには候補を出さない**のが 7.0.0 の中心的な性質です。

判定の作り方も変えました。Fable 5 と Codex に、**固定した根拠台帳（117件）だけ**を渡して
**16行 × 2プロファイル ＝ 32セル**を独立に監査させ、**1件ずつ人が裁定**しています。両者が一致した
セルも自動採用していません（実際に、両者が同じ読み落としをしていた2件を退けました）。

### Breaking

- **第二候補列を廃止しました。** 現行13行の第二候補のうち、**8件は今回の監査で「退けた候補」か
  「根拠が見つからない候補」**でした。残せば、根拠が無いと結論した組み合わせをそのまま提示する
  ことになるため、列ごと廃止し、根拠のあるものだけを3プロファイルへ吸収しています。
  - 提示の意味が変わります：**「代替」は無くなり**、目的（成果を優先するか、効率を優先するか）で
    候補が決まります。6.0.0 までの「代替＝別条件での候補」を前提にした使い方はできません。
- **タスク行を 13 → 16 に分割しました。** 旧「短い調査・要約・単純な質問」の1行を、
  **単純な質問／要約／短い調査／定型的な分類**の4行に分けています（分類タスクに固有の公式根拠が
  実在するため）。行の境界は判断表に明記しました。
- **headless の確定合図を変更しました。** `p` / `p normal`（通常推奨）・`p quality`（成果優先）・
  `p efficiency`（効率優先）・`p model <モデル> [<effort>]`（直接指定）・`n`。
  **旧 `p opus` / `p fable` / `p sonnet` は廃止**し、送ると書き方の案内を返します
  （6.0.0 の旧 `y` と同じ扱い）。effort は `low`〜`xhigh` のみ受理し、`max` は受理しません。

### Added

- **成果優先・効率優先のプロファイル**。成果優先はコストと待ち時間を評価から外し、効率優先は
  必要品質を保てる根拠がある範囲で総コストと所要時間を抑えます。選択肢は最大4つ
  （通常推奨／成果優先／効率優先／中止）です。
- **条件つきの候補**（6セル）。条件が成立するときだけ提示し、**成立しなければ通常推奨へ畳んで確定**
  （選び直しを求めません）、**判定できなければ候補を出さず条件を説明**します。
- **「根拠なし」の明示**（9セル）。差も同一性も裏付けられないセルには候補を作らず、その旨を表示
  します。「差の証拠が無い」と「差が無い証拠がある」を区別するためで、**候補を推測で埋めません**。
- 判断表に、各セルの**表示文・根拠 S-id・Confidence・留保**、および `—` としたセルの**探索範囲**を
  行ごとに記載しました。`SKILL.md` は説明文をここから取得します（文言を二重管理しません）。

### Changed

- **通常推奨のモデル×effort は、16行中1行だけ変えました**：**短い調査を `Sonnet 5 / low` →
  `Sonnet 5 / medium`** へ。この行は外部情報の検索・比較・出典付き統合を伴うと定義したため、
  公式が low の用例として挙げる範囲（`quick lookups`・`simple classification tasks`）より重いためです。
- **分岐D（Opus 5 の低 effort 活用）を通常推奨から削除**し、**効率優先のコードレビュー**へ
  条件つき候補として移設しました。コスト・速度を目的とする分岐を通常推奨に埋めたままだと、
  3プロファイルすべてに漏れ出すためです。利用者に見える変化は「**通常推奨から low が消え、
  効率優先を選んだときにだけ条件つきで出る**」ことです。
- **コードレビュー（大規模・独立監査）の効率側候補を `Opus 5 / xhigh` → `Opus 5 / high`** に
  改めました。従来は xhigh を選びながら「低い effort でも精度が保たれる」と説明しており、
  根拠と候補が噛み合っていませんでした。
- **UI/UX の Sonnet 5、長時間・自律実行の Opus 5 など、根拠を確認できなかった候補を取り下げました。**
  取り下げた理由は判断表に「探索範囲」として記録してあります。
- **`Other`（自由入力）で提示外のモデルを指定した場合は、ユーザーによる明示的な上書きとして扱い、
  推奨する根拠を作文しません。** 本体構文は `model <モデル> [<effort>]` で、headless の
  `p model …` と同じです。

### Fixed

- 選択肢の説明文に内部用語（`S<数字>`・分岐名・条件名）が混じる経路を塞ぎました。説明文の取得元を
  判断表の「表示文」欄だけに限定し、自分で書き足す案内文も禁止対象に含めています
  （検証中に実際に2件見つけたための修正です）。

### 根拠台帳（`01_sources_evidence.md`）

判断表を直す前に、**台帳そのものを全件監査して更新しました**（コミット `84d6768`）。

- **公式20ページを1回だけ取得して共通の生テキストに保存**し、その保存物に対して Fable 5 と Codex が
  独立に、**既存102件（Active 99 / Retired 3）を全件照合**し、**台帳に無い新規主張を探索**しました。
  候補は和集合で集め、**1件ずつ人が検証・承認**しています（自動昇格はしていません）。
- **S107〜S121 の15件を新設**しました（efficiency-first / capability-first の2方式、
  上げ下げヒューリスティック、総コストの逆転、レビュー・デバッグの recall、first-shot correctness、
  vision、指示追従、トークナイザ世代、知識カットオフ、稀な早期停止、モデル変更時の履歴再読、
  effort 変更とキャッシュ prefix、xhigh の典型用途 など）。**Active 114 ＋ Retired 3 ＝ 117件**。
- **既存10件を修正**しました（S2・S8・S11・S18・S50・S65・S75・S79・S83・S101）。主なものは
  **S8：「エラーで拒否」→ Fable 5 / Mythos 5 では 400 error**、**S83：vision 項の拡張**、
  **S75：Opus 4.8 からの移行でも high から再評価**、**S79：常設許可 → current session only**。
- **この版を `84d6768` で固定し、判断表の監査（32セル）はこの版だけを根拠にしています。**
  監査の途中で台帳が動くと、Fable と Codex が別の版を読むことになり、不一致が「解釈の違い」なのか
  「読んだ版の違い」なのか判別できなくなるためです。**3軸化の作業では台帳を変更していません。**

### 変更していないもの

- `p`（表示）／`g`（実行・一度だけ有効）の合図、長文入力の `p` 待ち、実行権の扱い、
  モデルを自動で切り替えない設計は 6.0.0 のままです。
- モデルの選定対象（Fable 5 / Opus 5 / Sonnet 5）と、Haiku 4.5 を対象外とする方針。

## [6.0.0] - 2026-08-11

候補の提示を選択UI（`AskUserQuestion`）に変え、合図を `p`（表示）と `g`（実行）に分けました
（**破壊的変更**のためメジャーを上げています）。**判断表の候補ペアと effort は変更していません。** ただし候補の意味を
正確に説明するため、第一候補を「通常の推奨開始点」、第二候補を「行ごとの別条件における代替」と
する文言の訂正を行っています（従来は「代替＝コスト・速度優先」と一般化していましたが、行に
よっては第二候補のほうが高能力・高価格であり、事実と合っていませんでした）。

### Breaking

- **合図を2つに分けました。`p`（prompt）は表示・再表示専用、`g`（go）は実行です。** 5.1.0 までの
  `y` は廃止し、モデル確定後またはプロンプト保留中に受け取っても実行・表示のどちらも行わず
  「`p`＝表示、`g`＝実行」と案内だけ返します
  （`yes` / `ok` / `go` / `run` / `prompt` も同じ扱い。**対話経路で受理する操作合図は完全一致の
  1文字だけ**です。headless の縮退経路だけは、モデル選択待ちに限り `p opus` 等を受理します）。

  理由は、下記の描画不具合と組み合わさると危険だったためです。プロンプトが生成されたのに画面に
  出ていない状態で、利用者が「見せて」のつもりで `y` を送ると、skill 側は「もう表示した」と
  認識しているため、**一度も読まれていないプロンプトで実行に進んでしまいます**。表示と実行を
  別のキーに分け、「はい」を連想させる `y` にはどちらも割り当てないことでこの経路を塞ぎました。

- **`g` は一度だけ有効です。** 受理した時点で**先に実行権を消費してから**実行を開始するため、
  同一セッションで状態が保持されている通常経路では、実行の途中失敗・中断のあとに `g` を再送しても
  二重実行しないよう設計しています（実行権は会話状態で管理しており、トランザクションのような
  厳密な保証ではありません。再試行は新しい依頼として送ってください）。
  **表示前に `g` を送った場合は、実行せずまず表示して停止します**（読まずに実行させないため）。
  実行後の `p` は再表示のみで、実行権は復活しません。**表示されたプロンプトをコピーして送って
  実行した場合も、その時点で実行権を消費します**（直後の `g` で二重実行しません）。新しい依頼を
  受け取った時点で、保留中のプロンプトと実行権は無効になります。

  `p` は表示済みの本文を**そのまま再掲するだけ**で、プロンプトを作り直しません（`g` が実行する
  対象を一意に保つため）。`g` は表示したプロンプトを元の依頼の範囲で実行する承認であって、
  破壊的操作への包括許可ではありません（通常の権限確認は実行中も適用されます）。

### Changed

- **フェーズ1の提示を選択UI（`AskUserQuestion`）に変更**しました。5.1.0 までは「ポップアップが
  出ると入力欄が塞がれ、モデル切替ができなくなる」という理由でテキスト提示にしていましたが、
  この懸念は解消しています。ポップアップはモデルを選ぶだけで閉じ、`/model` の設定はそのあと
  （プロンプト表示後）に行うためです。**選択UIより前に本文を出さない**ことを絶対規則に加えました。
- **長い入力では、選択後に最適化プロンプトを同じ応答で作らず `p` を待ちます。** 判定は入力の
  推定表示行数（各論理行の `文字数 ÷ 40` を切り上げて合計）が 32 行を超えるかどうか。**この 32 は
  実測値ではなく、実測境界（70〜80 行）から 13〜14 インチのノート相当へ外挿した値です。** 閾値を
  外しても壊れないよう、**選択UIには常に「表示されない場合は `p` を送ってください」と
  書いてあります**。閾値が決めるのは `p` が必須か保険かだけで、機能の成否ではありません。
- **フェーズ2から「元の指示」欄を削除**しました。利用者は自分の指示を把握しているため冗長です。
  ただし**表示から省くだけで、最適化処理からは外しません**。最適化後プロンプトは単独でコピーして
  実行できる自己完結形式で、元の要件・制約・対象・完了条件を保持します。
- `AskUserQuestion` が使えない環境（headless 等）では、従来のテキスト提示に自動で縮退します。
  この経路のモデル選択待ちに限り `p` / `p opus` / `p fable` / `p sonnet` / `n` を受理します
  （1文字規則の例外）。確定・表示後は対話経路と同じ `p`／`g` 文法に戻ります。単発の `claude -p`
  では表示後の `g` に到達しませんが、`--resume` 等の再開経路では到達しえます（複数ターン動作は
  引き続き保証外）。
- **「最適なモデル」という説明を「タスクと目的に合うモデル」に改めました。** 判断表の第一候補は
  当初から成果だけでなくコスト・速度・対話性を含めた実用上の推奨であり（根拠内訳に明記）、
  「最適」という表現は実態より強い主張でした。あわせて、**判断表にない基準（コスト度外視・
  成果優先など）での再選定は未定義**であることを README に明記しました。候補の意味を目的別
  （通常推奨・成果優先・効率優先）へ整理する作業は次版の課題です。
- **選択UIが自動で表示する `Other`（自由入力）の扱いを定めました。** `opus` / `fable` / `sonnet` の
  いずれかを指していれば受理し、それ以外は受理せず選び直しを促します（判定はやり直しません）。

### Fixed

- **長い依頼を貼り付けると、VS Code 拡張のチャットで提示が画面に出ない**問題を回避しました。
  実測で原因を特定しています：**貼り付けたブロックの描画高さがチャット画面に収まらないと、その
  直後の応答が描画されません。** 文字数やバイト数ではなく高さが引き金で、同じ 2,500 文字でも
  1 段落なら表示され、160 行に割ると消えます。同一ペイロードでウィンドウの高さだけを半分にしても
  消えます。生成・保存・`end_turn` は正常に完了しており、**スクロールでも会話の開き直しでも
  復帰しませんでした**。確認できた復帰方法は、新しい短いメッセージを送ることだけです。対話
  ターミナルでは再現しませんでした（9,621 文字で表示を確認）。VS Code 拡張 2.1.226 / 2.1.227 で再現。
  実測記録は [`tests/regression/vscode-render-threshold.md`](tests/regression/vscode-render-threshold.md)。

  これは拡張側の不具合であり、この skill では**選択UIがメッセージ一覧とは別の描画経路を通る**ことを
  利用して回避しています。近い上流報告は
  [anthropics/claude-code#61675](https://github.com/anthropics/claude-code/issues/61675)。
  4.0.0〜5.1.0 で「長文だと推奨が表示されない」と報告されていた症状は、これが原因でした。
  従来の説明（バイト数の閾値 2,012B / 4,391B）は代理変数を見ていたもので、**因果ではありません**。

## [5.1.0] - 2026-08-10

配布物をリポジトリ全体からプラグイン単体へ絞りました。**skill の動作・判断材料・呼び出し方は
変わりません。** 導入コマンド（`/plugin marketplace add` → `/plugin install`）も同じです。

### Changed

- **配布されるのは `plugins/which-model/` だけになりました。** これまで marketplace の `source` が
  `./` だったため、リポジトリ全体がプラグインとして配布されていました。実測では、インストール後の
  キャッシュに `CLAUDE.md`（このリポジトリの開発規約）・`docs/`（レシピ本の正本と画像4枚）・
  `tests/`・`tools/`・`install.sh` まで含まれ、**レシピ本は正本と同梱コピーの両方が配られて
  `01_sources_evidence.md` が2件存在していました**。プラグインをサブディレクトリへ隔離し、
  リポジトリルートの開発用ファイルが配布されないようにしました。

  | | 5.0.0 | 5.1.0 |
  | --- | --- | --- |
  | 配布ファイル数 | 31 | 10 |
  | キャッシュ容量 | 約 1,132 KiB | 約 208 KiB |
  | `01_sources_evidence.md` | 2 件 | 1 件 |

  測定条件：macOS / APFS、`du -sk`、実行中セッションが作るランタイムファイル（`.in_use/`、
  8ファイル・32 KiB）を除外。容量はファイルシステムの割当単位に依存するため参考値です。
  配布物の判定は**10ファイルの内訳**（`plugin.json` / `LICENSE` / `README.md` / `SKILL.md` /
  レシピ本6ファイル）で行っています。

- **更新手順の案内を3行に直しました。** カタログの更新（`/plugin marketplace update`）と
  プラグイン本体の更新（`/plugin update`）は別の操作で、従来の案内は前者しか書いておらず、
  実行しても新しいバージョンが入らない恐れがありました。

### Added

- **`LICENSE` をプラグインに同梱しました。** 配布単位が `plugins/which-model/` になったため、
  Apache-2.0 §4(a)（再頒布物へのライセンス本文の添付）を満たすようルートの `LICENSE` と
  バイト同一のコピーを置いています。
- **配布用の `README.md`（スタブ）を追加しました。** 説明・呼び出し名・リポジトリへのリンクのみで、
  リポジトリの `README.md` と二重保守しない方針です。
- **リリース前に `claude plugin validate` を2本とも通す規約を `CLAUDE.md` に追加しました。**
  marketplace 側の検証は marketplace manifest と `source` 先の `plugin.json` を検査しますが、
  `SKILL.md` などコンポーネント本体は検査しません（対照実験で確認）。片方だけでは不足します。

### 既存利用者の更新方法

サードパーティ marketplace の自動更新は既定でオフです。Claude Code の入力欄で次の3行を順に
実行してください。

```text
/plugin marketplace update kazuyakurashima
/plugin update which-model@kazuyakurashima
/reload-plugins
```

## [5.0.0] - 2026-07-25

2026-07-24 にリリースされた **Claude Opus 5** に対応し、モデル選定の対象を
**Fable 5 / Opus 5 / Sonnet 5** に更新しました。Opus 5 は挙動が Opus 4.8 から大きく変わっている
ため（thinking の既定、検証指示の扱い、応答の長さ、委譲の積極性）、判断表とプロンプト最適化の
ロジックも合わせて変更しています。**選定対象モデルとガイドのファイル名が変わるため、メジャー
バージョンを上げました。**

### Changed

- **選定対象を Fable 5 / Opus 5 / Sonnet 5 に変更しました。** 「迷ったら Opus」の既定が公式に
  Opus 5 へ移ったため（Models overview / Choosing a model）、判断表の Opus 列をすべて Opus 5 に
  差し替えました。**effort は `high`（既定）から始め、demanding なコーディング・エージェント作業
  （複数ファイル機能・大きめのリファクタ・端から端までの実装・深い依存チェーンの追跡）で `xhigh` へ
  上げる**設計にしました（公式の推奨開始点。Opus 4.8 / 4.7 は「xhigh 開始」でモデルにより異なります）。
- **`04_opus48_prompting.md` を `04_opus5_prompting.md` にリネームし、全面改訂しました。**
  Opus 5 の破壊的変更（thinking が既定 on、`thinking:{"type":"disabled"}` と effort `xhigh`/`max`
  の併用は 400 エラー）と挙動差分を反映しています。
- **最適化プロンプトに「重複する自己検証の指示」を入れなくなりました（Opus 5）。** Opus 5 は
  指示なしで自己検証・自己修正するため、公式が削除を求める3つの型（①最終検証ステップの追加
  ②検証用サブエージェントの起動 ③「double-check せよ」「答える前に再検証せよ」）は過剰検証を
  招きます。テンプレートから `Verification` 節を外し、品質の担保は完了定義（受入条件・通すべき
  テスト）の明示に一本化しました。**完了定義の明示と「推測に頼らずコードから確認せよ」型の
  根拠接地の指示は削除対象ではありません**（別工程の検証を足すものではないため）。
  **Fable 5 は従来どおり**、長時間実行では検証方法・間隔・別コンテキストの検証サブエージェントを
  明示します（公式ガイダンスがモデルで異なります）。
- **skill の実行契約を「ガイドの構成に従う」に変更しました。** 従来は全モデルで4要素
  （Fixed instruction / Variables / Output format / Verification）を必ず生成していましたが、
  テンプレートの構成をモデルガイド側に委ね、ガイドにない節を足さない方式にしました。
- **判断表に Opus 5 の低 effort 活用（分岐D）を追加しました。** Opus 5 の `low`/`medium` は旧
  Opus より強く、公式も「evals で品質が保てる範囲で積極的に使え」と述べています。ただし自前の
  評価で確認していない作業に常用しないよう `[Heuristic]` として条件を明示しています。
- **max effort のゲートをモデル別に書き分けました。** Opus 4.7 / 4.8 の「evals が `xhigh` で
  measurable headroom を示すときのみ」という明示ゲートは Opus 5 のセクションには存在せず、
  Opus 5 は「タスクが無制限のトークン支出に見合うとき」と表現されています。混同しないよう
  分離記載し、判断表で max を通常行に置かない方針は `[Heuristic]` として維持しました。

### Added

- **Opus 5 の運用注意を判断表に追加しました。** 応答・成果物が長い／スコープを広げがち／進捗
  ナレーションとサブエージェント委譲が多い、という癖への対処を「Opus 5 のプロンプト上の癖」節に
  まとめました。effort を下げても表示される応答は短くならない（長さはプロンプトで指定する）点も
  明記しています。
- **Claude Code の運用差分を追加しました。** Opus 5 は **v2.1.219 以降が必要**、`opus` エイリアス
  はプロバイダごとに解決先が異なる（Microsoft Foundry のみ Opus 4.6）、**Opus 5 には effort の
  default hold がなく前回値を持ち越す**（Fable 5 / Opus 4.8 / 4.7 は hold あり）ことを記載しました。
- **拒否時のフォールバック経路をサーフェス別・カテゴリ別に整理しました。** Claude Code では
  Fable 5 の biology → Opus 5、Fable 5 の cybersecurity → Opus 4.8、Opus 5 の cybersecurity →
  Opus 4.8、Opus 5 の biology はフォールバックなしで拒否。API 側はオプトイン（`fallbacks`）で、
  Fable 5 の推奨フォールバック先は現在も Opus 4.8 です。
- **プラン差の注記を追加しました。** Fable 5 は全有料プランで選択できますが課金形態が異なります
  （Max / Team Premium / legacy seat-based Enterprise Premium 席は**プラン内包・週間使用量上限の
  最大50%まで**、Pro / Team Standard 席は **usage credits** による従量課金）。「Pro では使えない」
  ではなく「usage credits が必要」と表現します。1M コンテキストは Sonnet 5 がネイティブ、
  Opus の 1M は Pro のみ usage credits が必要です。

### Documentation

- **根拠台帳に P16〜P21（What's new in Opus 5 / Prompting Claude Opus 5 / Model deprecations /
  Fast mode / API and data retention / Claude Fable 5 on your plan）と S81〜S106 を追加しました。**
  既存ページのうち **P1 / P2 / P5 / P7 / P9 / P11 / P12 / P13 / P14 / P15 を 2026-07-25 に直接取得で
  再検証**しました（P3 / P6 / P8 は今回未再取得で従来の検証日を維持、P4 は現在 P16 へリダイレクトされ
  直接取得不能、P10 は欠番）。
- **Opus 4.8 は「選定対象外・参照用」として台帳に残しました。** Opus 4.8 は公式に **Active**
  （暫定退役日は早くても 2027-05-28）で、フォールバック先・移行元・互換性の説明で参照するため、
  事実として正しい主張は削除していません。台帳の `Retired` は「この台帳の主張を置き換えた」と
  いう意味であり、モデルの廃止を意味しないことを明記しました。
- **ZDR の記述をモデル別・サーフェス別に精密化しました。** Covered Model（30日保持必須・ZDR 不可）
  は **Fable 5 と Mythos 5 のみ**です。Opus 4.8 は公式に "remains available under ZDR"、Sonnet 5 も
  ZDR 可。Opus 5 は Covered Model の指定がありませんが、ZDR 適格性は機能・サーフェス・組織契約に
  依存するため「全サーフェスで ZDR 可」とは書いていません。
- **公式ドキュメントの記述が同日内に変わった件を台帳に記録しました。** Opus 5 の effort 推奨は
  2026-07-25 の 06:54 時点の取得では「xhigh 開始」でしたが、同日 09:35 以降の再取得では
  「`high`（既定）開始・demanding な作業で `xhigh` へ昇格」になっていました（P2 / P13 / P16 / P17 の
  4ページ）。前後の逐語と取得時刻を「公式ページの記述が変わった記録」節に残しています。
  なお、この間に編集が行われたのか、先の取得が古い配信キャッシュだったのかは区別できないため、
  「公式が改訂した」とは断定していません。
- **`tools/CODEX_VERIFICATION_PROMPT.md` を全面更新しました。** 監査対象の P/S-id 範囲を
  P1〜P21 / S1〜S106 に更新し、既知の落とし穴に「Opus 4.8 と Opus 5 で方向が逆転している挙動」
  「thinking 既定の三者差」「Covered Model の "Both models" の読み違い」を追加しました。
- README のデモ用スクリーンショット 3 枚を 5.0.0 の画面で撮り直しました（代替候補が Opus 5 /
  effort=xhigh、`Last verified` が 2026-07-25 になっています）。代替テキストも実際の表示内容に
  合わせて書き直しました。

### Known limitations

- **貼り付けログによる汚染耐性は 4.1.0 から変わっていません。** 会話ログを丸ごと貼り付けて
  呼び出すと、ログ内の依頼や `y` に引きずられ、フェーズ1の提示を出さずに応答してしまう場合が
  あります。5.0.0 では `skills/pick/SKILL.md` の手順7b・7c を変更しましたが、**この観点の
  再測定は行っていません**（リリース前の確認は `tests/regression/pasted-transcript-input.txt` を
  1 回実行して不変条件を満たしたことの確認までで、成功率の測定ではありません）。
  4.1.0 候補での測定は 10 回中 8 回でフェーズ1を提示（2 回再発）。測定条件と履歴は
  `tests/regression/README.md` を参照してください。
- **対話型の Claude Code を対象としています。** headless（`claude -p`）での複数ターン利用は
  保証しません（`--session-id` / `--resume` を使えば動作しますが、サポート対象外です）。

## [4.1.0] - 2026-07-20

Anthropic 公式一次情報による effort ガイダンスの再監査を反映し、判断表を「第一・第二候補ごとの
effort」へ再構成しました。skill の実行契約も、モデルと effort の両方を扱う形に更新しています。

### Changed

- **判断表を第一候補・第二候補それぞれの effort に分離しました。** 従来の単一 effort 列を第一
  effort / 第二 effort の2列に分け、`medium〜high` のような範囲表記を明示的な分岐条件（分岐
  A/B/C）へ置き換えました。同名 effort はモデル間で同じ強度を意味しないため、モデルを替えるとき
  は effort もその行の値へ引き直します。
- **`y <model>` で確定 effort が連動します。** 第一/第二候補にそのモデルがあればその位置の
  effort を使い、行の候補に無いモデルなら既定の high にフォールバックしてその旨を表示します。
- **実行前の案内が `/model` と `/effort` の両方になりました。** effort は Claude Code でセッション
  をまたいで残るため、モデルと effort の両方を設定する運用に統一しました。

### Documentation

- 根拠台帳の複合 source_id `S7` を原子命題 `S71`〜`S76` に分割し、Claude Code の effort 運用
  （永続性・モデル間の非等価・ultracode / ultrathink の区別）と default-first 原則を `S77`〜`S80`
  として追加しました。max effort の公式な位置づけ（能力要件ベースの用途・Opus の eval ゲート・
  セッション限定）を明記し、判断表では max を通常の第一推奨に置かない方針を `[Heuristic]` として
  分離記載しています。
- クイックスタートで、ターミナル（PowerShell / コマンドプロンプト）と Claude Code の入力欄を明確に
  区別しました（スラッシュコマンドのコードブロックを `sh` から `text` へ変更）。あわせて、Claude Code の
  Native Install では Node.js が不要であること（npm 経由のときだけ Node.js 22+ が必要）と、未導入者向けの
  公式インストール導線、インストール後に `claude` で起動する手順、PowerShell で slash コマンドを打って
  しまったときのトラブルシューティングを追記しました。Claude Desktop の Code タブでもプラグインを導入
  できる点も明記しています。

### Known limitations

- **貼り付けログによる汚染耐性は完全ではありません。** 会話ログを丸ごと貼り付けて呼び出すと、
  ログ内の依頼や `y` に引きずられ、フェーズ1の提示を出さずに応答してしまう場合があります。
  4.1.0 候補での測定は 10 回中 8 回でフェーズ1を提示（2 回再発）。少数試行のため成功率や版間差
  としては解釈しません。測定条件と履歴は `tests/regression/README.md` を参照してください。

## [4.0.0] - 2026-07-17

**配布方法が変わります。** コピー方式（`install.sh` で `~/.claude/skills/` へ配置）をやめ、
Claude Code のプラグインとして配布します。これにより、更新の受け取りとアンインストールが
Claude Code の仕組みに乗ります。

### ⚠️ 破壊的変更

- **standalone のコピー方式から、Claude Code プラグイン配布へ移行しました。**
  導入は `/plugin marketplace add kazuyakurashima/which-model` →
  `/plugin install which-model@kazuyakurashima` になります。
- **正式な呼び出し名が `/which-model` から `/which-model:pick` に変わりました。**
  プラグインのスキルは名前空間つきで登録されるためです（`/pick` という短縮形も動きますが、
  公式に保証された呼び出し方ではないため、確実なのは `/which-model:pick` です）。
- **プロジェクト側の `docs/ai-model-guides/` は、実行時に読まれなくなりました。**
  判断材料はプラグインに同梱されたものだけを使います。プロジェクトごとの調整は 4.0.0 では
  できません（要望が確認できれば、明示的なオプトインとして再導入を検討します）。
- **`install.sh` は何もインストールしません。** 移行案内を表示して終了するだけのスクリプトに
  なりました。standalone 版を使い続ける場合は `git checkout v3.9.0` を使ってください。
- **旧 standalone 版とは共存できますが、別のコマンドとして読み込まれます**（`/which-model` と
  `/which-model:pick` が並びます）。standalone を外すと `/which-model` は使えなくなります。

### Added

- **判断材料（レシピ本）をプラグインに同梱しました。** インストール直後から、プロジェクトへの
  コピーなしで動きます。
- **フェーズ1の提示に、判断表の `Last verified` 日付を1行表示するようにしました。** モデルの
  仕様・価格は陳腐化します。更新が止まった版を使い続けても、古いことが分かるようにするためです。
- `tools/sync-bundled-guides.sh` を追加しました（`docs/ai-model-guides/` が正本。編集したら
  同梱コピーへ同期します。`--check` でズレを検出できます）。

### Changed

- **明示的に呼び出したときだけ起動するようになりました**（`disable-model-invocation`）。
  Claude が自動で判定を挟むことはありません。「重要なときだけ呼ぶ」という設計と実装を一致させました。

### Removed

- ルートの `SKILL.md` を `skills/pick/SKILL.md` へ移設しました。
- 旧 standalone 版の同期 hook（`.claude/settings.json` の PostToolUse）を削除しました。

### 既知の制限

- **長い指示に会話ログを貼り付けると、推奨モデルの提示を飛ばすことがあります。** 3.9.0 で対処
  しましたが**完全な解消ではなく**、確率的に再発します。4.0.0 の同梱一本化による明らかな劣化は、
  今回の3試行では観測されていません（`tests/regression/` に測定条件と記録があります）。
- **対話型の Claude Code を対象としています。** headless（`claude -p`）での複数ターン利用は
  保証しません（フェーズ2のガイド読み取りが権限で止まることを実測しています）。

## [3.9.0] - 2026-07-15

長い指示を渡したときに、推奨モデルが提示されないまま止まって見える問題への対応。
調査の結果、症状の異なる2つの問題が同じ「止まった」に見えていたことが分かりました。

### Changed

- **確定合図の受理条件を厳格化しました。** `y` / `y opus` / `y fable` / `y sonnet` は、
  メッセージ**全体**がそれだけで構成されているときにのみ確定合図として扱います。
  これまでは長文の一部に含まれる `y` も合図として解釈される余地がありました。
  `y お願いします` のように `y` で始まるが完全一致しない場合は、合図として受理せず、
  受理できる合図を再提示して停止します（直前の提示は有効なままなので、`y` を送り直せます）。
- **フェーズ1の判定を即断で行うよう指示を追加しました。** 判断表の該当行を引くだけの作業なので、
  指示が長くても本文の精査はしません。提示までの待ち時間を短くするためです。
- **重複した停止指示を削減しました。** 「やらないこと」節が絶対規則をほぼ再掲していたため、
  規則に含まれない補足のみを残しました。過剰指示は指示追従をかえって弱めます。

### Fixed

- **長文中に会話ログを貼り付けるとフェーズ1が飛ばされる問題に対処しました**
  （**完全な解消ではありません**）。貼り付けた過去の会話・モデル提案・`y`・ツール結果を、
  引用ではなく現在の会話状態として解釈してしまい、推奨モデルを提示せずに埋め込まれた指示へ
  回答することがありました。入力を分類対象のデータとして扱う規則0を追加しています。
  今回の再現試験では 3.8.0 の 0/3 提示から 3.9.0 の 9/10 提示へ改善しましたが、
  **再発を確認しています**（試行回数が少なく、成功率としては読めません。`tests/regression/`）。
- **フェーズ1の出力契約を明示しました。** 提示の4〜5行を本文として出力してから応答を終える
  ことを必須とし、本文なしで応答を終えることを禁じました。
- **README の「稼働中は読み取り専用なので、そもそも実行できません」という記述は誤りでした。**
  `allowed-tools: Read` は Read を事前許可する設定であり、他のツールを禁止するものではありません。
  提示だけで止まるのは SKILL.md の絶対規則によるもので、技術的な制約ではありません。

### Added

- README の「設計上の割り切り」に、提示までの待ち時間の実測範囲を追記しました。

### 補足：長さの上限が原因ではありません

900文字以上の実履歴18件のうち17件は、最終的に提案を返していました（8,357文字の成功例あり）。
提示までの待ち時間は数十秒〜2分超とばらつき、指示の文字数との単純な比例は見られませんでした。
セッションのコンテキスト量や effort も影響している可能性がありますが、確かめていません。
実際に提案が出なかったのは、会話ログを貼り付けた1件だけでした。
