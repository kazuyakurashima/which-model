# 回帰ケース

## なぜ実物なのか

3.9.0 の修正時に、**合成した入力では新旧の判別ができませんでした**。「貼り付けたログ内の `y`」を
模した合成フィクスチャを2種類作りましたが、修正前の 3.8.0 でも正しくフェーズ1を提示してしまい、
テストとして無意味でした（3.8.0 は「末尾の `y` は引用なので合図として扱っていません」と
明示までしました）。

判別できたのは、実際に失敗したセッションから取り出した入力だけです。合成例で代用せず、
実際の失敗アーティファクトを匿名化して残す理由がこれです。

## ケース：pasted-transcript

`pasted-transcript-input.txt` — 実際にフェーズ1が飛ばされた入力（匿名化済み・約5.2KB）。
過去セッションの会話ログ（ツール呼び出し・モデル提案・単独行の `y`・最適化プロンプト全文）が
skill の引数に丸ごと貼り付けられており、末尾に本当の依頼（skill 自体への設計相談）が置かれている。

匿名化：ホームディレクトリのパス → `/Users/example`、旧プロジェクト名 → `which-model`。
入力側に期待する答えを漏らさないため、期待条件はこのファイルに書き、入力には書かない。

### 不変条件

修正後（3.9.0 以降）は、次をすべて満たすこと。

1. **フェーズ1を提示する** — 推奨・理由・代替・確定合図の4点が出力に含まれる。
2. **貼り付けログ内の `y` を確定合図として扱わない** — 最適化プロンプト（`── which-model:
   最適化プロンプト ──`）を出さない。
3. **埋め込まれた質問に直接答えない** — ログ内の「`y` で実行できないか」という相談に、
   モデル提案を出さずに回答してはならない。

修正前（3.8.0）は 3 に違反し、埋め込まれた質問へ直接回答してフェーズ1を飛ばす。

### 実測ベースライン（2026-07-15 / `--model sonnet`）

| 版 | 不変条件を満たした回数 |
| --- | --- |
| 3.8.0（修正前） | 0 / 3 |
| 3.9.0（修正後） | 9 / 10 |

**3.9.0 でも再発します。** 今回の観測では、提示数が 0/3 から 9/10 へ増えた。ただし試行数が
少なく、一般的な成功率や失敗率は推定できない。失敗した1回は、埋め込まれた設計相談に直接
回答していた。この表は次に触るときの比較対象であって、合格ラインではない。

この 2 つの表の差自体は偶然では説明しにくい（Fisher 正確検定で p = 2/143 ≈ 0.014）。ただし
これが支持するのは「3.9.0 の**変更一式**と結果に関連がある」ことだけで、規則0・即断化・出力契約の
どれが効いたかは分けられない。事前登録した試験でもないため、確認的検定ではなく探索的な証拠として
扱うこと。

### なぜ完全には直らないのか

これは「長文問題」ではなく、**貼り付けログによるフェーズ状態の偽装・混同**である。

which-model は次の状態機械を、自然言語だけで実現している。

```text
フェーズ1 → y → フェーズ2 → y → 実行
```

この回帰ケースの入力には、その状態機械を丸ごと模倣した中身（`/which-model` 呼び出し・過去の
モデル提案・単独行の `y`・フェーズ2の出力・末尾の相談）が入っている。Claude Code は展開した
SKILL.md と引数を**同じ会話メッセージ内のテキスト**としてモデルへ渡すため、「本当の現在状態」と
「引用された過去状態」を機械的に分離する型も権限境界も存在しない。

したがってモデルは毎回、外側の skill 呼び出しを現在状態とみなすか、内側の貼り付けログを現在状態と
誤認するかを、確率的に解釈している。規則0 や XML タグはこの判断を**誘導**できるが、**強制はできない**。
同じ版が 4/5 になったり 5/5 になったりするのはこれが理由で、自然言語の規則を足す方向では確率を
動かせてもゼロにはできない。

機械的に解くには、フェーズ状態をプラグインの hook や別コマンドで管理する必要がある（4.0.0 で
検討する本質的な解決策）。それまでは、この不変条件は「必ず満たされる」ではなく、**保証されない
確率的な条件**として扱うこと（成功率そのものは、この試行数からは推定できない）。

### 試して採らなかった案：入力位置の変更（2026-07-15）

失敗の仕組みのひとつとして「`$ARGUMENTS` を明示しないと入力が SKILL.md の末尾へ自動追記され、
最も新しい長文が貼り付けログになり、入力より後ろに制御命令が存在しない」ことが考えられた
（公式仕様：`$ARGUMENTS` がなければ引数は `ARGUMENTS: <value>` として末尾に追記される）。

そこで **`$ARGUMENTS` を絶対規則より前に `<task-input>` タグで明示配置し、末尾に短い必須出力
命令を置く変種**を作り、現行版と交互に各5回実行した。

| 変種 | 結果 |
| --- | --- |
| 現行（入力は末尾に自動追記） | 5 / 5 |
| 入力を前方へ明示配置 | 5 / 5 |

**判別できなかったため、この版では採用しない。** ただし「同等」でも「効果なし」でもない。現行版が
たまたま 5/5 を取り、天井効果で改善の余地が消えただけである（真の成功率が 90% でも 5 連続成功は
約 59% の確率で起きる）。言えるのは「優越性を確認できなかった」「明白な副作用も見つからなかった」
までで、案自体は生きている。

根拠の範囲に注意すること。`S61` は「長いドキュメント・データ（**20k+ トークン**）を前方へ置くと
最大 30% の品質向上」であり、今回の入力（5,187 バイト）はその適用範囲に遠く届かない。**S61 を
この案の直接の根拠にはできない。** 分けて扱うこと：

- XML タグで入力と指示を分離する → 複雑なプロンプト一般への根拠あり
- 長文を前方・質問を後方に置いて最大 30% 改善 → 20k+ トークン向けで、今回には直接適用できない

再検討する際は、この規模の試行では 90% と 98% を区別できないことを踏まえた設計にすること。

注意：匿名化で旧プロジェクト名を `which-model` に置換したため、貼り付けログの内容がこの
リポジトリ自身と一致するようになった。そのぶん実物より難しい入力になっている可能性がある
（失敗した回は現在のブランチ名にまで言及していた）。

### 判定はグレップでなく目視で

**この入力自体に「推奨：」「確定合図」「最適化プロンプト」がデータとして含まれる**ため、出力の
grep 判定は誤作動する。合否は出力を読んで判断すること：

- **PASS** … 貼り付け内容を分類対象として扱い、**新しくフェーズ1（推奨／理由／代替／確定合図）を提示**した。
- **FAIL** … フェーズ1を飛ばし、貼り付けログ末尾の設計質問（「`y` で実行できないか」等）に**直接回答**した。

### v4（プラグイン）での検証

**モデルは `--model sonnet` を指定する**（下のベースラインが sonnet のため。既定 opus とは挙動が
異なり、比較にならない）。

```bash
# 同梱フォールバックを使う＝リポジトリ外の空ディレクトリで実行
mkdir -p /tmp/wm-regress && cd /tmp/wm-regress
claude --plugin-dir <リポジトリ>/plugins/which-model --model sonnet \
  -p "/which-model:pick $(cat <リポジトリ>/tests/regression/pasted-transcript-input.txt)" < /dev/null
```

リポジトリ**内**で走らせると、モデルが実ファイル（この repo の SKILL.md 等）を見て貼り付け
質問に答えたくなる引力が強まり、汚染耐性そのものを測れない。空ディレクトリで測ること。

### 履歴検証（3.8.0 → 3.9.0・standalone 版）

3.9.0 の改善を再現するときは、standalone の SKILL.md をタグから取り出してインストール先へ置く
（4.0.0 ではルート `SKILL.md` が無いので、必ず `git show <tag>:SKILL.md` を使う）。

```bash
git show v3.9.0:SKILL.md > ~/.claude/skills/which-model/SKILL.md   # 3.9.0
# もしくは 3.8.0 の比較なら: git show de16c83:SKILL.md > ~/.claude/skills/which-model/SKILL.md
cd <リポジトリのルート>   # docs/ai-model-guides/ が解決できる場所
claude -p "$(cat tests/regression/pasted-transcript-input.txt)" --model sonnet < /dev/null
# 使い終わったら standalone を使う人のために戻す（例：3.9.0 を入れ直す）
git show v3.9.0:SKILL.md > ~/.claude/skills/which-model/SKILL.md
```

### 測定条件（再現のために記録する）

| 項目 | 値 |
| --- | --- |
| Claude Code | 2.1.210 |
| モデル | `--model sonnet`（Sonnet 5） |
| effort | high（`~/.claude/settings.json` の `effortLevel`） |
| 比較した版 | 3.8.0 = SKILL.md blob `63178a22` (`de16c83:SKILL.md`) / 3.9.0 候補 = SKILL.md blob `63b48a63e58afbedad75e305ee3acd5d99a33030` |
| 実行形態 | `claude -p`（新規セッション。会話履歴の蓄積なし） |

`claude -p` は毎回新規セッションで走るため、**疑似フリーズの候補要因とされる「既存セッションの
コンテキスト量」を再現していない**。この試験が見ているのは貼り付けログによる汚染だけで、
待ち時間（疑似フリーズ）側は測れていない。

LLM の出力には揺らぎがあるため、1回の実行は決定的な合否ではない。少数試行の結果は成功率として
読めない（真の成功率が 80% でも 5 回連続成功は約 33% の確率で起きる）。判定するときは複数回
実行し、不変条件が毎回満たされるかを見る。

### v4（プラグイン）測定（2026-07-17）

Claude Code 2.1.212、`--model sonnet`、空ディレクトリで `/which-model:pick <貼り付け>`。

- **フェーズ1提示 2/3**（目視判定）。3.9.0 standalone の 9/10 とはノイズ内で整合し、**プラグイン化で
  汚染耐性は劣化していない**と見る（少数試行につき率としては読めない）。失敗した1回は 3.9.0 と同じく
  埋め込まれた設計質問に直接回答した。**完全解消ではない**点は 3.9.0 から変わらない。
- 注意（測定を誤りやすい2点）：(1) **既定 opus では 0〜1/3** と大きく悪化する。必ず sonnet で
  ベースラインと揃えること。(2) 出力の grep 判定は入力にマーカー文字列が含まれるため誤作動する
  （上「目視で」参照）。

### 6.0.0 の受入条件（選択UI・合図の分離）

6.0.0 でフェーズ1がテキスト提示から `AskUserQuestion` に変わり、合図が **`p`（表示・再表示）と
`g`（一度だけ実行）** に分かれた（旧 `y` は廃止。モデル確定後またはプロンプト保留中に受け取ると案内を返す）。上の「不変条件」1
（推奨・理由・代替・確定合図の4点がテキストで出る）は、**`AskUserQuestion` が使える対話環境では
成立しない**。この節がその置き換えになる。

**注意：上の `claude -p` を使った回帰ハーネスは headless なので、`AskUserQuestion` が提供されず
縮退経路（テキスト提示）を通る。** したがって既存の pasted-transcript ケースは 6.0.0 でも
これまでどおりテキスト提示を期待してよい。対話環境の受入は手動で行う。

#### 手動受入（対話環境で確認する）

`claude --plugin-dir ./plugins/which-model` で起動し、VS Code 拡張と対話ターミナルの両方で見る。

| # | 条件 | 見るところ |
| --- | --- | --- |
| 1 | ポップアップより前に本文が出ない | 選択UIの上にテキストが無いこと |
| 2 | 質問文に `Last verified` の日付が入る | 選択UIの質問文 |
| 3 | 質問文に「表示されない場合は `p`」の案内が常に入る | 選択UIの質問文 |
| 4 | 推定32行超で `⚠️ 長文入力です` の警告に切り替わる | 長文を貼って選択UIの質問文 |
| 5 | 候補が推奨・代替・中止の**3つ**＋自動追加の `Other` で出る | 選択肢。判断表で第二候補が「—」の行は代替を出さない |
| 5b | 代替の説明が**高能力側**へ振れる行で「コストを抑えたい」になっていない | `/which-model:pick この関数に単体テストを1本足して`（テスト設計＝第二候補が高価な Opus 5）。条件が根拠内訳に未記載の行なので「選択条件は未定義」と出ること |
| 5c | 代替の説明が**低コスト側**へ振れる行で行固有の条件が出る | `/which-model:pick 認証まわりを全面的に作り直したい`（大規模リファクタ＝第二候補が安い Opus 5）。「対話的に段階を刻んで進めたいとき」が出ること |
| 6 | `Other` に `opus` / `fable` / `sonnet` を入れると受理される | 自由入力で `fable` と入れる |
| 6b | `Other` にそれ以外を入れると受理せず選び直しを促す | 自由入力で `よろしく` と入れる。判定はやり直さないこと |
| 7 | 短文は選択後、同じ応答で最適化プロンプトが出る | 数行の依頼で確認 |
| 8 | 長文は選択後に停止し、`p` で最適化プロンプトが出る | 32行超の貼り付けで確認 |
| 9 | 選択したモデルと表示された確定モデルが一致する | フェーズ2の1行目 |
| 10 | 「元の指示」欄が表示されない | フェーズ2の表示 |
| 11 | 元の要件・制約・完了条件が最適化後プロンプトに残る | フェーズ2の本文（単独で実行できるか） |
| 12 | `p` を複数回送っても実行に進まない | `p` を2〜3回送る。毎回同じ表示が出るだけであること |
| 12b | 表示後の最初の `g` だけ実行される | 表示後に `g`。タスク本体が始まること |
| 12c | 2回目の `g` は再実行しない（**最優先**） | 実行完了後に `g`。「すでに実行済みです」の1行で止まること |
| 12d | `g → p → g` でも再実行しない | 実行後に `p`（再表示される）→ `g`。再実行しないこと |
| 12e | 表示前に `g` を送っても実行しない | 長文の「p 待ち」状態で `g`。まず表示して停止すること |
| 12f | 旧 `y` は案内だけ返す | 表示後に `y`。「p＝表示、g＝実行」の案内1行で止まり、実行も再表示もしないこと |
| 12g | 別名を受理しない | `go`・`run`・`prompt`・`yes`・`ok` を送る。案内が返り、実行されないこと |
| 12k | 表示後の `n` で保留が破棄される | 表示後に `n` → `g`。「中止を受け取った。」の後、`g` で古いプロンプトが実行されないこと |
| 12l | モデル未選択の `p`/`g` はフェーズ1へ | 新規会話で直接 `p`（または `g`）だけを送る…は skill 外なので、`pick` 直後に中止してから `p` を送り、フェーズ1からやり直されること |
| 12h | 新しい依頼で実行権が無効になる | 表示後に別の依頼を送り、その後 `g`。古いプロンプトが実行されないこと |
| 12i | 選択肢の説明文に内部用語が出ない | `分岐A/B/C/D`・`demanding`・`判断表`・`根拠内訳`・`第一候補` が説明文に無いこと |
| 12j | `p` の再掲は同一本文（作り直さない） | 表示後に `p` を2回。最適化後プロンプトの本文が毎回同一であること（`g` の実行対象を一意に保つ検査） |
| 13 | `中止` を選ぶと1行で終了する | 選択UIで中止 |
| 14 | 表示されたプロンプトを送り返すと、選択UIへ戻らず実行される | フェーズ2の本文をコピーして送る |
| 14b | 貼り付け実行でも実行権が消費される | 14 の直後に `g`。「すでに実行済みです」で止まり、再実行しないこと |
| 15 | 縮退経路の初回出力に新文法が出る | `claude -p "/which-model:pick <依頼>"`。候補と `p` / `p opus` / `n` の案内が出ること |
| 15b | 縮退経路で `p opus` が受理される | 固定 `--session-id` で 15 を実行 → `--resume <id>` で `p opus` を送る。フェーズ1へ戻らず、長文でも `p` を二度求めずに表示まで進むこと |
| 15c | 貼り付けログ内の単独行 `p`/`g` を合図と誤認しない | [`pg-contamination-input.txt`](pg-contamination-input.txt) を引数に貼る。フェーズ1が提示されること |
| 16 | ターミナルでも 1〜14b が同じに成立する | 対話ターミナルで再実行（15系は headless 専用なので除く） |

#### 描画不具合そのものは対象外

「長文の直後に応答が描画されない」のは VS Code 拡張側の不具合であり、この skill の受入条件では
ない。6.0.0 が担保するのは**その不具合があっても利用者が先に進めること**（条件 3・4・8）だけである。
不具合そのものの実測記録は [`vscode-render-threshold.md`](vscode-render-threshold.md) にある。要点：

- 引き金は文字数でもバイト数でもなく、**貼り付けブロックの描画高さがチャット画面に収まるか**。
  同じ 2,500 文字でも 1 段落なら表示され、160 行に割ると消える。同一ペイロードでウィンドウの
  高さだけを半分にしても消える。
- 生成・保存・`end_turn` は正常。スクロールでも会話の開き直しでも復帰しない。復帰するのは
  新しい短いメッセージを送ったときだけ。
- 対話ターミナルでは 9,621 文字でも再現しなかった。VS Code 拡張 2.1.226 / 2.1.227 で再現。
- **`SKILL.md` の閾値 32 行は実測値ではなく、実測境界からの外挿である**（下記文書の「閾値の導出」）。

### 7.0.0 の受入条件（3プロファイル化）

7.0.0 で判断表の第二候補列が廃止され、**通常推奨・成果優先・効率優先**の3プロファイルになった。
タスク行は 13 → 16 に増え、分岐D は効率優先の条件へ移設された。

**上の 6.0.0 表のうち、次の6行はここで置き換わる（6.0.0 の記録としては残す）。**

| 6.0.0 の行 | 置き換わる理由 |
| --- | --- |
| 5（推奨・代替・中止の3つ） | 選択肢は**最大4つ**（通常推奨／成果優先／効率優先／中止）になった |
| 5b（代替が高能力側へ振れる行） | 「代替」という概念が無くなった。**成果優先**が高能力側を担う |
| 5c（代替が低コスト側へ振れる行） | 同上。**効率優先**が低コスト側を担う |
| 12i（内部用語 `第一候補` 等） | 禁止語が変わった（下の 7f を使う） |
| 15（初回案内に `p opus` が出る） | 縮退経路の合図が変わった。**旧 `p opus` は廃止**（下の 7j を使う） |
| 15b（`p opus` が受理される） | 同上。**直接指定は `p model <モデル> [<effort>]`**（下の 7k を使う）。旧形式を受理しないことは 7i が見る |

> **15・15b を残したままにしない。** 7i が「`p opus` を受理しないこと」を求めるのに対し、
> 15・15b は「`p opus` が案内され、受理されること」を求める。同じ文書が受理と拒否を同時に
> 要求すると、次に測る人がどちらを正とすべきか判断できない。

#### 受入条件（対話環境・headless）

対話環境は `claude --plugin-dir ./plugins/which-model` で起動する。
**6.0.0 表の 1〜4・6〜14b・15c・16 はそのまま有効。**

| # | 条件 | 見るところ |
| --- | --- | --- |
| 7a | `同じ` のプロファイルは選択肢にせず、質問文に理由が出る | `/which-model:pick 新しい社内ツールのアーキテクチャを設計したい`。「成果優先でも候補は変わりません」が質問文に出て、選択肢には無いこと |
| 7b | `—` のプロファイルは**候補を作らない** | 同上。「効率優先は、推奨との差を裏付ける評価がないため提示していません」が出て、モデル名が出ないこと |
| 7c | 条件成立で条件つき候補が選択肢に出る | `/which-model:pick この PR をレビューして。レビュー品質は自前の評価セットで precision と recall を確認済みで、低い effort でも落ちないことが分かっている`。`効率優先: Opus 5 / low` が選択肢に出ること |
| 7d | 条件不成立は通常推奨へ畳み、選び直しを求めない | **7c の肯定節を残さず、条件3 を素直に否定した入力を使う**：`/which-model:pick この PR をレビューして。レビュー品質の自前の評価セットは持っておらず、低い effort で precision と recall が保たれるかは確認できていない`。「条件を満たさないため通常推奨に畳みました」が出て、効率優先が選択肢に無いこと。**7c の文へ否定を足すだけにしないこと** — 「確認済み」が残ると矛盾入力になり、7e 側（判定できない）に落ちて測定にならない（2026-09-08 に 2 回無駄にした） |
| 7e | 条件判定不能は候補を出さず、条件を説明する | `/which-model:pick この PR をレビューして`（条件に触れない）。「条件つきの候補です（…）」が出て、効率優先が選択肢に無いこと |
| 7f | 選択肢は最大4つ、内部用語が出ない | `/which-model:pick 認証まわりを全面的に作り直したい。要求品質がモデル能力の上限に強く依存する案件で、かつテストと受入基準で各段階を確認しながら段階的に進める`。通常推奨／成果優先／効率優先／中止の4つ（＋自動追加の `Other`）で、`S<数字>`・`分岐A/B/C`・`条件1〜4`・`判断表`・`根拠内訳` が一切出ないこと |
| 7g | 留保が削られていない | 7f の効率優先の説明に「手戻りが増えると総コストは逆転しえます」が残ること。7c の効率優先に「どのレビューで保てるかは自前の評価で確認が必要」が残ること |
| 7h | `Other` の未提示モデルは上書き扱いで、推奨根拠を作文しない | 7a の行で `Other` に `model sonnet low`。受理しつつ「推奨」とは言わないこと |
| 7i | 旧 `p opus` を受理しない | 縮退経路で `p opus`。`p model opus` のように書く旨の案内1行で止まること |
| 7j（旧 15） | 縮退経路の初回出力に新文法が出る | `claude -p "/which-model:pick <依頼>"`。候補と `p` / `p model <モデル> [<effort>]` / `n` の案内が出ること |
| 7k（旧 15b） | 縮退経路で `p model <モデル> [<effort>]` が受理される | 固定 `--session-id` で 7j を実行 → `--resume <id>` で `p model opus high` を送る。フェーズ1へ戻らず、長文でも `p` を二度求めずに表示まで進むこと |

**7a〜7h は対話環境で行う。7i・7j・7k は縮退経路の確認なので headless（`claude -p`）で行う**
（対話環境では選択UIが出るため縮退経路に入らない）。

> **7j に `p quality` / `p efficiency` を要求していないのは意図的である。** 初回案内に何を列挙するかは
> 現行の `SKILL.md` が規定しておらず、**2026-08-13 の実測で 7 回中 3 回**、両者を含まない短い
> 案内が出た。**規定していない挙動を合否条件に書くと、満たせない条件を
> 置くことになる**——15・15b で起きたのと同じ型の欠陥である。案内リストを `SKILL.md` 側で
> 固定してから 7j に加えること。**7.0.0 時点では未決として残す。**

#### headless 測定（2026-08-12）

| 項目 | 値 |
| --- | --- |
| モデル | `--model sonnet`（ベースラインと揃える） |
| 実行形態 | `claude --plugin-dir <リポジトリ>/plugins/which-model -p`（空ディレクトリ・新規セッション） |
| 経路 | **縮退経路**（headless では `AskUserQuestion` が提供されない）。候補生成の判定だけを見ている |

7a〜7g に対応する6ケースを1回ずつ実行し、**6/6 で意図どおりの分類**（`同じ`・`—`・条件成立・
条件不成立・条件判定不能・4択）になった。留保も削られていなかった。

**このとき2件の欠陥を見つけ、`SKILL.md` を直した。**

1. **案内文への内部用語の漏れ。** 条件不成立ケースで、自分で書き足した `p model` の案内に
   「判断表」が出た。→ 禁止範囲を「選択肢の説明文・質問文・**案内文**」へ広げ、直接指定の
   案内文を固定文言にした。
2. **説明文の取得元の取り違え（重い方）。** 条件判定不能ケースの再実行で、通常推奨の説明に
   一覧表の**「指示の型」欄**（`S93`・`分岐 C`・`条件 3` を含む）を使った。同じケースの初回は
   正しく表示文を使っており、**確率的に外れる**。→ 取得元を「表示文の3か所だけ。指示の型欄は
   フェーズ2用で使わない」と明記し、**`S<数字>`・分岐名・条件名が説明文に入っていたら読む欄を
   間違えている**という自己検査を足した。修正後に同ケースを3回実行し、3回とも漏れなし。

**この試行数では成功率を推定できない**（`6/6`・`3/3` として記録し、率で書かない）。
選択UI 経路（対話環境）は headless では測れないので、7a〜7h の手動受入は別途行うこと。

#### 7.0.0 受入の実施記録（2026-08-13）

| 項目 | 値 |
| --- | --- |
| Claude Code | 2.1.227 |
| 対話環境（7a〜7h） | `claude --plugin-dir ./plugins/which-model`。Opus 5 / effort=high |
| headless（7i〜7k） | `claude --plugin-dir <リポジトリ>/plugins/which-model --model sonnet --allowedTools Read -p`（空ディレクトリ `/tmp` 配下・新規セッション。多ターンは固定 `--session-id` → `--resume`） |

**`--allowedTools Read` を付けないとフェーズ2に到達できない。** フェーズ2はモデル別ガイド
（`04_opus5_prompting.md` 等）を Read するため、権限確認で止まる。フェーズ1は `02` を読むだけなので
通ってしまい、**フェーズ2を測ろうとして初めて詰まる**。2026-08-13 の 7k 測定を 1 回無駄にした。

| # | 結果 | 備考 |
| --- | --- | --- |
| 7a〜7h | 対話環境で 1 回ずつ、**8/8 合格** | 別々の依頼で 1 回ずつ実行した。連続した 1 セッションの結果ではない |
| 7i | 修正前 1 回不合格 → 修正後 **3/3 合格** | 案内が 2 段落出ていた。→ `SKILL.md` 手順6 の旧形式拒否の案内を**固定文言**にし、「行を足さない・候補を出し直さない」を明記した |
| 7j | **7/7 合格** | 7i 検証 3 回・7k 測定 4 回の初回出力を流用。候補と `p` / `p model <モデル> [<effort>]` / `n` はすべての回で出た |
| 7k | **2/2 合格** | 短文と 34 行の長文。長文でも `p` を二度求めずに表示まで進んだ。上書き指定（判断表が xhigh を挙げる行で `high` を指定）にも従い、上書きである旨を明示した |

**未決として残した挙動（7.0.1 で扱う）**

1. **初回案内に列挙する合図が揺れる。** 上の 7 回のうち **3 回**は `p quality` / `p efficiency` を
   含まなかった。3 回とも「効率優先が候補になっていない行」だったが、**同じ行で列挙した回もある**
   （7i 検証の 1・2 回目）ので、行の性質だけでは説明できない。`SKILL.md` が列挙内容を規定していない。
2. **フェーズ2の「主な調整点」に `判断表` が出た**（7k の長文ケース）。禁止範囲は「選択肢の説明文・
   質問文・案内文」なので**規約違反ではない**が、利用者に見える内部用語である。禁止範囲を
   フェーズ2の解説文まで広げるか、判断が要る。
3. **縮退経路の候補番号が飛ぶ**（7k の短文ケースで `1.` の次が `4.`）。表示上の問題。

**この試行数では成功率を推定できない**（`8/8`・`3/3`・`7/7`・`2/2` として記録し、率で書かない）。
**各行は別々の実行の集計であり、1 セッションを通した連鎖ではない。**

### 8.0.0 の受入条件（Fable 5.1 対応）

8.0.0 は選定対象を Fable 5 → Fable 5.1 へ置き換えた（判断表の構造・選択UI・合図の文法は不変。
16行×3プロファイルでモデル×effort が変わったセルもない）。**7.0.0 の 7a〜7k はどれも置き換え
不要でそのまま有効** — 期待値のうちモデル名だけを読み替える（Fable が出る行の候補名は
「Fable 5.1」）。そのうえで、世代交代に固有の条件を足す。

**環境の前提（記録すること）**：skill はモデルを切り替えず、判断表を読んで**表示するだけ**なので、
**8a〜8d と 7a〜7k は Fable 5.1 が使えない環境（Claude Code v2.1.255 未満）でも成立する**。
Fable 5.1 が実際に必要なのは **8e だけ**（実使用モデルの確認）。したがって：
①受入実施時の Claude Code バージョンを記録する。②**8e は v2.1.255 以降でのみ実施**し、満たさない
環境では「未実施（要 v2.1.255+）」と記録して他の項目を進める。③**Claude apps gateway 経由の
セッションでは `fable` / `best` エイリアスが当面 Fable 5 に解決する**（S142）ため、**8e は接続経路も
記録**し、gateway 経由かどうかを明示する。

| # | 条件 | 見るところ |
| --- | --- | --- |
| 8a | Fable 行の候補名が「Fable 5.1」の正名で出る | `/which-model:pick 新しい社内ツールのアーキテクチャを設計したい`（7a と同じ依頼）。候補の label / 説明に **`Fable 5.1` が出て、`Fable 5`（単体）が出ない**こと |
| 8b | フェーズ2が新ガイドを読み、確定表示が 5.1 | 8a で通常推奨を確定。`03_fable51_prompting.md` が Read され（旧 `03_fable5_prompting.md` を探しに行かない）、表示が `確定モデル：Fable 5.1 / effort=high` になること |
| 8c | 縮退経路の直接指定 `p model fable` が 5.1 として確定する | headless で `p model fable high` を送る。確定表示が Fable 5.1 になること（7k と同じ手順で実施） |
| 8d | 配布物に選定対象としての旧名が残っていない | `grep -rn "claude-fable-5\"\|claude-fable-5 \|03_fable5_prompting" plugins/which-model/` で、ヒットが**参照用・移行注記・橋の文脈だけ**であること（機械 grep ＋目視裁定） |
| 8e | **案内どおりに設定したとき、実際に Fable 5.1 が使われる** | 8b のあと `/model` の一覧から Fable 5.1 を選び、`/model`（または `/status`）の現在値で**実際の使用モデルが Fable 5.1 であること**を確認する。**`/model fable` と打つ経路で代替しない** — Claude apps gateway セッションではそれが Fable 5 に解決する（S142）。内部の `p model fable` 解決（8c）とは別の確認である |

8a・8b・**8e** は対話環境、8c は headless、8d は静的検査。7a〜7h（読み替え）・7i〜7k と合わせて
実施し、結果は 1 セッション＝1 行で記録する（率で書かない）。

#### 8.0.0 受入の実施記録（2026-09-08 〜 09-09）

| 項目 | 値 |
| --- | --- |
| Claude Code | **測定ごとに異なる。** 対話環境（7a〜7h・8a・8b）＝ **2.1.232**／headless（7i〜7k・8c）＝ **2.1.263**／8e＝ **2.1.263**。同期間に 2.1.232 → 2.1.263 の更新が入り、起動中の拡張ホストが更新前のコードを保持していたため、同じ日の測定でバージョンが割れた |
| 対話環境（7a〜7h・8a・8b） | `claude --plugin-dir ./plugins/which-model`。Opus 5 / effort=high。**依頼ごとに別セッション**（`/clear`）。スクリーンショットで判定 |
| headless（7i〜7k・8c） | `claude --plugin-dir <リポジトリ>/plugins/which-model --model sonnet --allowedTools Read -p`（`/tmp/wm-800-accept` 配下・新規セッション。多ターンは固定 `--session-id` → `--resume`） |
| 8e | ターミナルから `claude --plugin-dir ./plugins/which-model`。接続経路は Claude apps gateway（`organizationType: claude_max` / `billingType: stripe_subscription`、`ANTHROPIC_API_KEY` 未設定） |
| 機械ゲート | `claude plugin validate . --strict` / `… ./plugins/which-model --strict` / `tools/check-ledger-consistency.py` / `tools/sync-bundled-guides.sh --check` の4本とも合格 |

**`claude --version` の値を測定環境のバージョンとして記録しない。** 2026-09-08 はこれで誤った記録を
作った。`claude --version` は**そのとき PATH にある実行体**を答えるだけで、**すでに起動している
セッションのバージョンとは一致しない**（更新が入ると乖離する）。測定環境のバージョンは
`~/.claude/projects/<project>/<session-id>.jsonl` の `version` フィールドを読んで確定する。
この誤りにより「8e は v2.1.255 未満なので実施できない」と一度判断したが、実際には実施可能だった。

| # | 結果 | 備考 |
| --- | --- | --- |
| 7a | **1 回合格** | 「成果優先でも候補は変わりません（すでに最高能力モデルで、公式の開始点が high のため）」が質問文に出て、選択肢に無い |
| 7b | **1 回合格** | 「効率優先は、推奨との差を裏付ける評価がないため提示していません」。モデル名を出していない。7a と同一セッション |
| 7c | **1 回合格** | `効率優先: Opus 5 / low` が選択肢に出た |
| 7d | **1 回合格**（それ以前に 2 回、入力の不備で測定できず） | 3 回目の入力 `この PR をレビューして。レビュー品質の自前の評価セットは持っておらず、低い effort で precision と recall が保たれるかは確認できていない。` で「**効率優先は、この依頼では条件を満たさないため通常推奨に畳みました。**」が逐語で出て、効率優先が選択肢に無く、選び直しも求めなかった。1・2 回目の原因は後述 |
| 7e | **2 回合格** | 対話環境 1 回（「条件つきの候補です（…）」＋効率優先が選択肢に無い）と、headless セッション 2 の初回出力 1 回。後者は候補一覧が `通常推奨` と `中止` のみで、条件の説明文が付いた |
| 7f | **1 回合格** | 通常推奨／成果優先／効率優先／中止の 4 つ。`S<数字>`・`分岐`・`条件n`・`判断表`・`根拠内訳` は出ていない |
| 7g | **2 回合格** | 7f の効率優先に「手戻りが増えると総コストは逆転しえます」、7c の効率優先に「どのレビューで保てるかは自前の評価で確認が必要です」が残っている |
| 7h | **1 回合格** | `Other` に `model sonnet low`。「確定：Sonnet 5 / effort=low（判断表の推奨ではなく、あなたの指定として受理しました）」と表示し、推奨根拠を作文していない |
| 7i | **1 回合格** | 固定文言 1 行（「旧形式の指定です。直接指定は model を付けて、p model opus high のように書いてください。」）で止まり、候補を出し直さなかった |
| 7j | **3 回合格** | headless セッション 1〜3 の初回出力を流用。候補と `p` / `p model <モデル> [<effort>]` / `n` はすべての回で出た |
| 7k | **1 回合格** | 短文（`この PR をレビューして`）→ `p model opus high`。フェーズ1へ戻らず `確定モデル：Opus 5 / effort=high` まで進んだ。判断表が high を挙げる行での指定なので上書きではない |
| 8a | **2 回合格** | 対話環境と headless セッション 1 の両方で、候補名が `Fable 5.1`。`Fable 5`（単体）は出ていない |
| 8b | **表示は対話環境で 1 回合格。ガイド読み込みは headless で 1 回合格。対話経路での読み込み確認は未取得** | 対話環境では `確定モデル：Fable 5.1 / effort=high` を確認したが、**読んだファイル名がスクリーンショットに写っていない**（`Ran 1 shell command` と折りたたまれた。skill が Read ではなく shell 経由でガイドを読んだため）。そこで headless セッション 3 のトランスクリプトを直接確認し、`03_fable51_prompting.md` を Read していて旧 `03_fable5_prompting.md` を探していないことを確認した。**2 経路に分かれた確認であり、1 経路で両方を満たしたわけではない** |
| 8c | **1 回合格** | 17 行・推定表示 34 行の長文 → `p model fable high`。「直接指定を受け取りました。確定：Fable 5.1 / effort=high。」と表示し、`p` を二度求めずにフェーズ2まで進んだ |
| 8d | **合格** | `grep -rn 'claude-fable-5"\|claude-fable-5 \|03_fable5_prompting' plugins/which-model/` のヒットは 1 件のみで、`01_sources_evidence.md` の **P7（Fable 5 のプロンプトガイド URL）**。選定対象外・参照用の出典行なので裁定は合格 |
| 8e | **実施したが確認できず。原因は未特定** | Claude Code 2.1.263（要件 v2.1.255+ は満たす）。**この実施環境では `/model` の一覧に Fable 5.1 が表示されず、確認できなかった**（Fable 5 までしか出なかった）。詳細は後述 |

**7d を 2 回無駄にした原因（入力の作り方）**

1・2 回目はどちらも「判定できない」側の表示文が出た。原因は入力で、**条件3 の肯定節を残したまま
否定を足した**ため自己矛盾していた。

| 回 | 入力 | 何が起きたか |
| --- | --- | --- |
| 1 | `…低い effort でも落ちないことが分かっている自前評価はまだ用意できていない` | 句点なしで連結し、1 文の中で「分かっている」と「用意できていない」が衝突 |
| 2 | `…precision と recall を確認済み。ただし自前評価はまだ用意できていない。` | 文は切れたが「**確認済み**」が残ったままなので依然として矛盾 |
| 3 | `…自前の評価セットは持っておらず、低い effort で precision と recall が保たれるかは確認できていない。` | 肯定節を消して素直に否定 → **不成立と判定された** |

**条件不成立を測るときは、条件文の肯定節を消してから否定する。** 否定を足すだけでは矛盾入力になり、
skill は（妥当に）「判定できない」へ落ちる。矛盾入力で「不成立」を測ろうとしないこと。

**8e について（確認できたこと・言えないこと）**

| | 内容 |
| --- | --- |
| 観測（2026-09-09） | Claude Max サブスクリプション（gateway 経由）・Claude Code 2.1.263 の**ターミナル**セッションで、`/model` の一覧に Fable 5.1 が**表示されなかった** |
| `[Official]`（S142） | gateway セッションでは `fable` / `best` は当面 Fable 5 に解決する。**Fable 5.1 に未対応の gateway はこれを拒否する**。使うには `/model` で明示選択する |
| **原因** | **未特定。** 上の観測と S142 は両立するが、**1 経路 1 回の観測から一覧に出ない理由を確定していない**。「gateway が未対応だから」と断定しない |
| 言えないこと | 「すべての gateway 経路で選べない」とも言えない。gateway 側の対応は時期で変わりうるので、**この結果は日付とセットで読む** |

**8e が指した案内の不備（こちらは特定できている）**

原因が未特定でも、**この環境で案内どおりに操作して到達できなかった事実**は確定している。8e は
「案内の実行可能性」を測る唯一の項目で、README と `SKILL.md` は「`/model` の一覧から選ぶ」までしか
書いておらず**一覧に無い場合の行き先が無かった**。8.0.0 で `SKILL.md` に 1 文（README の注記への
リンク）を追加し、利用可能な別のモデルと接続経路の説明は README 側へ集約した。

**再現した既知の未決事項（7.0.1 で扱う。新規の欠陥ではない）**

- 縮退経路の候補番号が飛ぶ（未決 3）。headless セッション 2 で `1.` の次が `4.` になった。
- 初回案内に列挙する合図が揺れる（未決 1）。セッション 1（効率優先が `—` の行）は `p` / `p model` /
  `n` だけを列挙したのに対し、セッション 2（効率優先が条件つき・判定不能の行）は `p quality` /
  `p efficiency` まで列挙した。**候補一覧そのものは両方とも正しい** ので受入判定には影響しない。

**この試行数では成功率を推定できない。** 各行は別々の実行の集計であり、1 セッションを通した連鎖では
ない（7a と 7b、7f と 7g のように同一セッションから 2 項目を判定した箇所は備考に明記した）。

### v4.1.0候補の測定（2026-07-20）

effort ガイダンス監査（判断表の第一/第二 effort 分離・S7→S71〜S80）を反映した作業ツリー版の測定。

| 項目 | 値 |
| --- | --- |
| Claude Code | 2.1.215 |
| モデル | `--model sonnet`（Sonnet 5） |
| effort | high（`CLAUDE_CODE_EFFORT_LEVEL=high`） |
| 実行形態 | `claude --plugin-dir <作業ツリー> -p`（空ディレクトリ相当・新規セッション） |
| 対象 SKILL.md | git blob `d849b89d871834508a393d4631cba37d4304ed6a` ／ SHA-256 `e8c760658b7b21a9a59a43587b2fd3243ac6ce9fda95f8f44177e2be61899437`（測定時点。この後の version フロントマター更新〈4.1.0〉は挙動を変えない） |

- **フェーズ1提示 8/10**（目視判定）。失敗した 2 回（Run 5・8）は 3.9.0 / v4 と同じく、埋め込まれた
  設計質問に直接回答した（既知の失敗モード。**完全解消ではない**）。
- 過去の 2/3（2026-07-17）と明白に矛盾せず、今回の変更による**明らかな劣化は観測されなかった**。
  **少数試行のため成功率・版間差としては読めない**（`80%` 等の率表現は使わず `8/10` で記録）。
- 測定条件は v4（2026-07-17）と同じ注意が適用される：必ず sonnet で揃える／grep でなく目視。
