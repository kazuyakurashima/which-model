---
name: model-router
description: |
  Claude Code でコーディング指示を受けたとき、その内容（設計・要件定義・実装・
  リファクタ・レビュー・デバッグ・テスト・UI/UX・ドキュメント・調査）と複雑さを判定し、
  最適な Claude モデル（Fable 5 / Opus 4.8 / Sonnet 5）と effort レベルを理由つきで提示する。
  提示後は一度停止し、ユーザーが手動でモデルを切り替えてから y を送ると、選ばれた
  モデル向けにプロンプトを最適化して即座に実行する。
  「どのモデルを使うべきか」「このプロンプトを最適化して」「モデルを提案して」等のときに使う。
allowed-tools: Read
metadata:
  version: "3.1.0"
  author: "kaz"
  last-updated: "2026-07-05"
---

# model-router

指示内容から最適なモデルと effort を提示し、`y` を受けたら推奨モデル向けに
プロンプトを最適化して実行する。判断材料はこのリポジトリの `docs/ai-model-guides/`
にある（このファイルには判断材料を書かない。必ず読んで判断する）。
背景・設計理由・セットアップ手順は README.md を参照（人間向け。動作には不要）。

## 動作フロー

### フェーズ1：判定と提示（ここで必ず一度止まる）

1. ユーザーの指示を次のタスク種別に分類する：
   設計 / 要件定義 / 実装（通常）/ 実装（難所）/ 大規模リファクタ / レビュー /
   デバッグ / テスト / UI・UX / ドキュメント / 調査・要約。
2. 複雑さ（軽い・普通・重い）を見積もる。
3. `docs/ai-model-guides/02_model_selection_matrix.md` を Read し、判断表に照らして
   推奨モデルと effort を決める。
4. 次を簡潔に提示する（4〜5行）：
   - 推奨：<モデル名> / effort=<レベル>
   - 理由：<タスク種別と複雑さに基づく一文>
   - 代替：<第二候補>（<選ぶ状況>）
   - 切替：`/model <モデル名>`（推奨に変えるなら先にこれを実行。不要ならそのまま）
   - 実行：準備ができたら `y` を送ってください
5. ここで応答を終える。ポップアップ（AskUserQuestion）は出さない
   （出すと入力欄が塞がれ /model 切替ができなくなるため）。ユーザーの次の入力を待つ。

### フェーズ2：`y` を受けたら最適化して即実行

6. ユーザーが `y`（または「実行」「go」等の明確な実行合図）を送ったら、確認や
   再質問を挟まず、直ちに次を行う：
   a. 対象モデル（直前に提示した推奨モデル）のガイドを Read する：
      - Fable 5 → `docs/ai-model-guides/03_fable5_prompting.md`
      - Opus 4.8 → `docs/ai-model-guides/04_opus48_prompting.md`
      - Sonnet 5 → `docs/ai-model-guides/05_sonnet5_prompting.md`
   b. そのガイドの「再利用テンプレート」（Fixed instruction / Variables /
      Output format / Verification）に沿って元の指示を再構成する。
   c. そのガイドの「悪いパターン（避ける）」に該当する表現を除く。特に Fable 5 では
      「思考を出力せよ」「推論を説明せよ」系を必ず除く。
   d. 再構成したプロンプトでそのまま実行に移る。追加の確認は出さない。
7. ユーザーが `n`・「やめる」等を送ったら、実行せず終了する。

## 判断の土台（詳細は 02 の判断表を正とする）

- 迷ったら Opus 4.8（複雑なエージェント型コーディングの既定）。
- 最高能力・長時間・曖昧・端から端まで → Fable 5。速い対話には使わない。
- 速い対話・高頻度・低コストの主力 → Sonnet 5。
- モデルを変える前に effort を上げることを検討する。
- 機微データで ZDR 前提なら Fable 5 を避け Opus 4.8（Fable 5 は ZDR 不可）。

## やらないこと（境界）

- `y` を受け取る前に、勝手に実行しない。
- `y` を受けたら確認や再質問を挟まず即実行する。
- モデル切替の確認・警告を出さない（ユーザーの自己責任）。
- ポップアップ（AskUserQuestion）を出さない。
- 判断材料をこのファイルに書き足さない。`docs/ai-model-guides/` を正とする。
