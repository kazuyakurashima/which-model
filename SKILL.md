---
name: model-router
description: |
  Claude Code でコーディング指示を受けたとき、その内容（設計・要件定義・実装・
  リファクタ・レビュー・デバッグ・テスト・UI/UX・ドキュメント・調査）と複雑さを判定し、
  最適な Claude モデル（Fable 5 / Opus 4.8 / Sonnet 5）と effort レベルを理由つきで提示する。
  提示後は一度停止し、ユーザーが手動でモデルを切り替えてから y を送ると、選ばれた
  モデル向けにプロンプトを最適化して即座に実行する（最適化内容は既定では非表示、
  -v 指定時のみ表示）。
  「どのモデルを使うべきか」「このプロンプトを最適化して」「モデルを提案して」等のときに使う。
allowed-tools: Read
metadata:
  version: "3.4.0"
  author: "kaz"
  last-updated: "2026-07-05"
---

# model-router

指示内容から最適なモデルと effort を提示し、実行合図を受けたら実行モデル向けに
プロンプトを最適化して実行する。判断材料はこのリポジトリの `docs/ai-model-guides/`
にある（このファイルには判断材料を書かない。必ず読んで判断する）。
背景・設計理由・セットアップ手順は README.md を参照（人間向け。動作には不要）。

## 絶対規則（最優先・例外なし）

- **このskillが呼ばれたら、いかなる指示内容であっても、まずフェーズ1（判定と提示）を
  必ず最初に実行する。** 指示が具体的な作業依頼に見えても、提示を省略して作業を
  開始してはならない。
- **「レビュー」「精査」「調査」「分析」「確認」「まとめて」等、それ自体が実行可能な
  作業に見える指示でも、例外ではない。** まず提示して停止する。作業には入らない。
- **フェーズ2（実際の作業・実行）は、ユーザーが実行合図（`y` 等）を送った後にのみ
  開始する。** それ以前に、対象ファイルの精査・コード生成・調査などの実作業を一切
  始めない。判断表（02）を読むこと以外の作業をしない。
- この規則は、指示の緊急性・具体性・作業性のいずれによっても上書きされない。

## 動作フロー

### フェーズ1：判定と提示（ここで必ず一度止まる）

1. ユーザーの指示を次のタスク種別に分類する：
   設計 / 要件定義 / 実装（通常）/ 実装（難所）/ 大規模リファクタ / レビュー /
   デバッグ / テスト / UI・UX / ドキュメント / 調査・要約。
2. 複雑さ（軽い・普通・重い）を見積もる。
3. `docs/ai-model-guides/02_model_selection_matrix.md` を Read し、判断表に照らして
   推奨モデルと effort を決める。（Read してよいのはこの判断表のみ。指示対象の
   ファイル群や外部情報を、この段階で読み込んで作業を始めない。）
4. 次を簡潔に提示する（4〜5行）：
   - 推奨：<モデル名> / effort=<レベル>
   - 理由：<タスク種別と複雑さに基づく一文>
   - 代替：<第二候補>（<選ぶ状況>）
   - 切替：`/model <モデル名>`（推奨に変えるなら先にこれを実行。不要ならそのまま）
   - 実行：推奨のままなら `y`。代替に変えたら `y opus` / `y fable` / `y sonnet` の
     ように実行モデル名を添える。最適化内容も見たいときは `-v` を付ける（例：`y opus -v`）
5. ここで応答を終える。ポップアップ（AskUserQuestion）は出さない
   （出すと入力欄が塞がれ /model 切替ができなくなるため）。ユーザーの次の入力を待つ。

### フェーズ2：実行合図を受けたら、実行モデルを確定し最適化・実行

6. ユーザーの実行合図を解釈する（順不同で読み取る）：
   - `y` → 実行モデルは、直前に提示した推奨モデル。
   - `y opus` / `y fable` / `y sonnet` → 実行モデルはその指定モデル（推奨と違っても指定を優先）。
   - `-v` または `--verbose` または「見せて」等が含まれる → 詳細表示モード（下記 d を実行）。
   - `n`・「やめる」等 → 実行せず終了する。
7. 実行モデルが確定したら、確認や再質問を挟まず、直ちに次を行う：
   a. 実行モデルのガイドを Read する：
      - Fable 5 → `docs/ai-model-guides/03_fable5_prompting.md`
      - Opus 4.8 → `docs/ai-model-guides/04_opus48_prompting.md`
      - Sonnet 5 → `docs/ai-model-guides/05_sonnet5_prompting.md`
   b. そのガイドの「再利用テンプレート」（Fixed instruction / Variables /
      Output format / Verification）に沿って元の指示を再構成する。
   c. そのガイドの「悪いパターン（避ける）」に該当する表現を除く。特に Fable 5 では
      「思考を出力せよ」「推論を説明せよ」系を必ず除く。
   d. **詳細表示モード（-v 等）のときだけ**、実行前に次を表示する：
      - 実行モデル：<確定した実行モデル> / effort=<レベル>
      - 元の指示：<ユーザーの元指示を1〜2行に要約>
      - 最適化後：<再構成したプロンプト全文>
      - 主な調整点：<どの観点で変えたかを1〜2行>
      （-v が無ければ、この表示はしない。最適化は内部で行い、結果だけ返す。）
   e. 最適化後プロンプトでそのまま実行に移る。追加の確認・停止はしない。

## やらないこと（境界）

- 実行合図を受け取る前に、勝手に実行・作業しない（指示が作業依頼でも同じ）。
- フェーズ1で、判断表（02）以外のファイルや情報を読み込んで作業を始めない。
- 実行合図を受けたら、確認や再質問を挟まず即実行する。
- **最適化内容を既定で表示しない**（`-v` 等が指定されたときのみ表示）。
- **ユーザーが明示的に頼まない限り、成果物をファイル化しない**。実行結果は
  チャットに返す。「ファイルにして」「保存して」等の指示があるときだけ書き出す。
- モデル切替の確認・警告を出さない（ユーザーの自己責任）。
- ポップアップ（AskUserQuestion）を出さない。
- 判断材料をこのファイルに書き足さない。`docs/ai-model-guides/` を正とする。

## 判断の土台（詳細は 02 の判断表を正とする）

- 迷ったら Opus 4.8（複雑なエージェント型コーディングの既定）。
- 最高能力・長時間・曖昧・端から端まで → Fable 5。速い対話には使わない。
- 速い対話・高頻度・低コストの主力 → Sonnet 5。
- モデルを変える前に effort を上げることを検討する。
- 機微データで ZDR 前提なら Fable 5 を避け Opus 4.8（Fable 5 は ZDR 不可）。
